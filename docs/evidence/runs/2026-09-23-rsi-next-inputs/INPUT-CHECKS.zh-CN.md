# 后续数据：原始文件已取得，划分与目标校准仍待完成

**后续纠正：本页保留最初六项原始检查。单车和超导已有研究使用，不能作为新任务。** 新增两项替代数据、完整历史核查和划分前分组见 [GROUP-CHECKS.zh-CN.md](GROUP-CHECKS.zh-CN.md)。原 `downloads.json` 与 `raw-audit.json` 没有改写。

九个原始文件全部下载成功，共 937,153,329 字节；来源、固定提交、SHA-256 和公开 MD5 核验见 `downloads.json`。没有发送研究模型请求、训练基线或运行 GPU 容器。

`audit.py` 已实际执行并退出 0，检查结果见 `raw-audit.json`。它只读原始文件，检查结构、标签、重复和泄漏关系；不执行远端数据加载代码。未生成后续实验的训练/开发/确认/审计划分，未选择或运行研究候选。

| 数据 | 原始规模 | 发现与后续要求 |
|---|---:|---|
| EMNIST Balanced | 112,800 train + 18,800 test | 两组完全重复图像，无跨原划分重复；交换图像末两轴后，人工核看六个字符的方向与标签一致。 |
| SVHN | 73,257 train + 26,032 test | 一组完全重复图像具有冲突标签，无跨原划分重复；原标签 10 映射为数字 0，保留冲突记录并归组。 |
| Banking77 | 10,003 train + 3,080 test | 规范化文本有 12 组重复，其中 7 组跨原 train/test。后续划分须避免这些组同时进入训练和验收。 |
| DBpedia 14 | 560,000 train + 70,000 test | 8 组重复标题、18 组重复正文跨原 train/test；13 组重复标题有标签冲突。需联合标题/正文的重复关系归组。 |
| Bike hourly | 17,379 行、731 个日期 | 每行 `casual + registered == cnt`；这两列必须排除。候选输入有 10 组完全重复特征，8 组标签不同；日期组与重复特征组需合并后划分。 |
| Superconductivity | 21,263 行、81 个输入特征 | 两张表的目标逐行一致。归一化组成只有 15,164 组；其中 2,375 组存在不同临界温度。需联合组成及相同特征归组，保留测量差异。 |

重复和标签冲突没有被静默删除。原 train/test 来源不等于所有派生输入天然无交叉；分组规则还须在后续实际划分中检查。图像来源近似重复、材料未记录的物理条件、文本改写等相关性也不能靠完全相等的哈希排除。

这些检查避免明显的数据泄漏，但不证明研究目标可达、候选有效或通用研究能力提高。六个公开数据任务之外，后续仍需定义并检查优化问题任务；生成实例必须明确标识。任何目标可达性校准都应发生在双方首个研究请求前，并保留失败记录。

原始文件留在 `/hot-data/fsx/workspace/erik/work/orze-p1-continuation-20260914/rsi-next-inputs-20260923/raw/`，仓库归档保存来源、校验、脚本和检查结果。数据来源与许可见 [SOURCES.zh-CN.md](SOURCES.zh-CN.md)。
