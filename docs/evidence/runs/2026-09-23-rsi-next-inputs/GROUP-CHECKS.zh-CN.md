# 纠正旧数据重用，并完成划分前分组

本页保留分组阶段记录；后续四份划分及独立核验已经完成，见 [PREPARED.zh-CN.md](PREPARED.zh-CN.md)。

原检查只扫描 `tasks.json`，漏掉了 9 月 18 日已用的 Bike Sharing 与 Superconductivity。扩大到两仓 396 份历史记录后确认旧使用；`novelty-review.json` 保留具体计划、行号和哈希。两项已移出后续新任务池，原九文件下载和原始检查保持不变。此处纠正发生在下一轮任何候选或基线运行之前。

从 UCI 下载 YearPredictionMSD、Online News Popularity 两份替代原始归档，共 218,488,382 字节，两次下载均成功。来源与校验见 `regression-downloads.json`。选择依据是未找到历史使用、领域覆盖与数据可用性，没有候选分数参与。

`group_inputs.py` 已实际执行并退出 0，耗时 27.68 秒。它将相同输入或相关记录连成组，并再次检查每条原始关系都落在同一最终组；保留所有行和不同目标，不用标签决定分组。输出仅为评估器内部元数据，尚未切分训练/开发/确认/审计，也没有提供给研究模型。

| 数据 | 行数 | 连通组数 | 主要处理与边界 |
|---|---:|---:|---|
| EMNIST Balanced | 131,600 | 131,598 | 同像素归组；保留官方测试池，不排除近似重复。 |
| SVHN | 99,289 | 99,288 | 同像素归组；一组冲突标签保留。 |
| Banking77 | 13,083 | 13,071 | 7 组横跨原 train/test；这些完整组后续必须留在验收侧。 |
| DBpedia 14 | 630,000 | 629,825 | 联合标题和正文相等关系；26 组跨原划分，13 组有不同类别，最大组 15 行。 |
| YearPredictionMSD | 515,345 | 515,129 | 90 个音色特征，同输入归组；两组年份冲突保留，无跨原划分完全重复。保留官方艺术家隔离边界，内部艺术家 ID 缺失。 |
| Online News Popularity | 39,644 | 720 | 联合日期、URL、相同输入关系；最大组 105 行。排除 12 个分享量聚合特征、URL、采集间隔和目标，剩余 46 个输入。 |

新闻原文件实际为 39,644 行，与 UCI 网页的 39,797 不同，按原文件记录。该任务每个日期组内分享数不同是正常现象，`conflicting_target_groups` 对日期组的计数不代表标注错误。目标原值从 1 到 843,300，是否变换及最终损失尚未冻结；未通过目标截断或删异常值制造改善。来源特征可能由整个语料生成，不能把现成特征回归宣称为完整发布前预测系统。

这些分组只是防止明显交叉的前置步骤，不能证明隐藏相关性已经消失，更不证明研究目标可达或候选有效。六个分组文件的路径和哈希见 `grouping.json`，位于原准备目录 `groups/`，不把原始数据或样本目标重新发布到仓库。优化问题、共同参考、目标校准、最终划分和候选仍待完成。期间新增研究模型请求 0、GPU 执行 0。
