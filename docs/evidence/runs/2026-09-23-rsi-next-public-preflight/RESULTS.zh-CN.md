# 六个公开数据集共同参考已完成，尚非研究能力对照

运行前冻结 `plan.json`，在 EMNIST Balanced、SVHN、Banking77、DBpedia 14、YearPredictionMSD、Online News Popularity 上各做两次划分，共 12 个实例。来源、分组、私有行序和历史使用范围沿用已归档的新输入准备；这里不声称基础模型从未见过这些数据。

## 完成情况

- **24/24 参考训练有效，12/12 共同参考已保存**。实际使用全部八张 GPU，分固定波次；每 GPU 同时最多一个本次容器。
- **36/36 独立预测容器完成**：每个所选参考分别重载开发、确认、审计特征。预测时没有挂载标签或训练数据。
- 60 个容器全部闭合、退出 0；校验 36 份开发预测的独立宿主评分，源码与全部输入哈希保持冻结。
- 分类重载逐元素一致；回归按运行前预定 `rtol=atol=1e-12` 核验。没有为实际结果修改容差。
- 仅按两种参考的开发损失选择，平局选 v0。最终标签未用于选择、评分或改变阈值。
- **新增模型研究请求 0，最终研究验收成功 0**。此次只完成共同起点和执行可重复性验证。

完整墙钟约 3.75 分钟，是预检执行时间，不是研究目标达标时间。

## 所选参考的开发损失

| 任务 | 划分 0 | 划分 1 | 指标 |
|---|---:|---:|---|
| EMNIST Balanced | 0.119000 | 0.118000 | 分类错误率 |
| SVHN | 0.049500 | 0.057500 | 分类错误率 |
| Banking77 | 0.096429 | 0.102143 | 分类错误率 |
| DBpedia 14 | 0.022500 | 0.018500 | 分类错误率 |
| YearPredictionMSD | 84.610038 | 83.882300 | MSE |
| Online News Popularity | 0.734150 | 0.758844 | log1p 分享数 MSE |

图像沿用同一残差 CNN 的 SGD/AdamW、100 epochs/650 秒上限；图像语义禁止水平翻转。文本沿用词/字符 TF-IDF LinearSVC，回归沿用 600 轮 HGB/700 棵 ExtraTrees。每容器 2 CPU、12GiB 内存、约 6GiB PyTorch 显存上限，训练 900 秒、完整划分预测 600 秒上限。

固定镜像只在原依赖上新增此前核验的 OR-Tools 与 immutabledict；公共参考不使用这两项。训练源码保持先前兼容性检查选择的方法，没有据将来的候选表现削弱参考。图像两种优化器在不同划分各有被选中；文本 Bank77 选择字符，DBpedia 选择词；回归均选择 HGB，完整另一种结果保留。

## 后续边界

这些是开发损失，不能当作最终泛化成绩，也不能证明每个任务的预定改善幅度都可达。已保存的确认/审计预测用于将来对照的共同参考；本阶段没有读取相应标签做效果选择。

下一轮仍缺优化目标可达性证据、完整实际默认流程的公平对照及前瞻协议。研究候选尚未在这些新任务上启动，不能宣称均值胜出、generic 能力提高或 30% 目标完成。检查和预检没有 API 费用，最新费用账本仍是原生两轮诊断的 $4627.321975 保守累计占用、$372.678025 未分配。
