# 两组共同运行环境准备完成，实测等待独占GPU

30%研究验收时间目标未完成，产品默认不变。必要的CPU准备已完成，当前无法继续GPU预检或完整前瞻比较。

## 本轮实际完成

- 从上一轮已经逐一核对PyPI SHA256的wheel中，只安装新增8个包到任务专用目录，1113文件、58,712,012字节。原有Enroot根目录和已有服务没有修改；两组都经同一个executor只读挂载到`/mnt`并设置相同`PYTHONPATH`。
- 在实际原生Enroot根目录执行CPU探针：PyVRP 0.14.0加载自该只读目录；NumPy 2.3.4和PyTorch 2.9.1仍加载自原环境。1113文件哈希一致；四客户小实例与已核验Docker校准环境的预测、整数路线成本、浮点评分完全一致。
- 实测PID1、只读根目录和依赖目录、仅lo网络、无宿主路径/凭证、capabilities为0、NoNewPrivs=1。CPU探针使用两个逻辑CPU亲和性及12GiB RLIMIT_AS，**不等同于Slurm内存cgroup，也没有GPU执行**。
- 首次探针将包挂载到不存在的`/extra`而启动失败，记录保留。第二次使用原根目录已存在的`/mnt`和`/candidate`挂载点后退出0，不修改原冻结根目录来消除失败。
- 原生adapter仅新增共同库挂载和版本/只读检查。独立逆向差异核对确认GPU所有权、CUDA计算、6GiB上限、入队、预算末排空及独立验收逻辑保持原样；相同实际配置解析器再次核对600秒角色超时、20分钟停滞、30秒poll和900秒cooldown。
- 两个STL-10实例的6个逻辑预检步骤已准备，仍用原两参考和原划分。预期4次训练加6次独立预测，共10个worker；申请4张GPU，每步2CPU/12GiB，最终按开发选择、不评分确认/审计标签。代码、依赖和输入1152文件已绑定。**未启动，没有执行结果。**

## 当前阻塞及审计

连续三个目标回合均检查到同一GPU资源问题；前两个回合在等待条件下完成CPU参考和CVRP校准，本轮补齐共同环境。现在剩余关键工作都依赖可独占GPU。

当前Slurm的main/a100是同一组48个节点，没有idle GPU节点。五个MIXED节点虽有空闲CPU，但`AllocMem == RealMemory`，不能再分配所需内存。其他可运行节点已分配；其余为down/drain。没有本任务活跃Slurm作业。过去的八GPU申请1325680已在600秒后超时闭合，不重启它或在相同状态下反复提交。

本机八张A100均有其他服务的计算进程，显存占用约31–68GiB。瞬时GPU利用率0不等于设备可独占。本任务不停止这些服务、不修改设备所有权检查，也不把共享设备或CPU结果替代原生GPU证据。

阻塞解除需要当前队列能分配独占GPU，或可访问的另一台已授权GPU机器。图像预检可先使用4张，完整对照按既定设计使用8张。既有模型调用授权和$5000上限仍有效，不要求重复授权或创建新监控。

## 恢复后的执行顺序

1. 核对实际GPU分配、进程身份、共享依赖文件哈希，执行已准备的STL-10预检并独立核验。
2. 验证新增共同依赖在实际GPU worker中的可用性及两原生组绑定。
3. 用全部16实例共同参考冻结新的四领域/八任务/两重复/两组协议和预算，再执行32次真实模型研究。
4. 只有满足至少30%更低的最终验收墙钟均值、原领域区间/成功率/过程完整性门槛并确认真实默认入口绑定，才考虑升级默认并完成目标。

目前参考14/16，新增模型调用0、GPU执行0、研究最终验收0。预算仍为累计保守占用$4659.467075、未分配$340.532925；26条未知记录继续保留。不得用环境探针通过或此前CVRP路径成本改善代替研究效率证据。
