# 确认选择开发对照：首个完整配对与机制核查

状态：研究仍在进行；本记录只包含前三个闭合实例，不能作为完整均值、默认决定或新前瞻能力证据。

- KMNIST 第一次配对两组均通过：control 2.345 分钟，candidate 6.895 分钟。候选最终错误率 1.62%，control 1.90%，共同参考 2.32%；更好的预测质量没有转化为更快验收交付。
- candidate 的首个完成方法 b1-s0 已达开发阈值（0.004），随后花 234.03 秒执行另一方法并选择其较低开发损失（0.0035）。首份方法没有确认/审计结果，不能据此宣称这 234 秒必然可省且成功率不变。两组独立生成了不同方法，273.06 秒配对差也不能全部归因于确认规则。
- Abalone candidate 2.888 分钟通过，最终 MSE 改善 10.1495%。但第一份开发损失 4.28555 高于 4.20945 门槛，第二份 4.20252 才合格；按相同已观察顺序，立即确认规则也会选择第二份。因此这次成功没有证明延迟确认的因果收益。
- Abalone 两方法的内部验证仍有明确限制：在同一套基础模型 OOF 预测上再次交叉拟合集成权重，并非完整嵌套验证；选中方法先用全部训练特征确定裁剪分位数，且内部“参考”在工程特征上重训，与共同原始特征参照不完全相同。最终验收仍独立使用冻结共同参考，不能把内部估计当独立证据。
- 选中 Abalone 方法报告了 MLP 参数错误并排除该成员；另一方法静默跳过失效成员。源码列出方法不等于真正执行成功。已有集成仍通过外部验收，这些局部错误保留，不改写成功数。

三条闭合记录的确认选择、源文件/检查点归属通过现有核验函数；11 份开发/确认/审计预测及参考的点损失用独立 Python 求和重算一致。没有执行候选源码、加载检查点、发出额外模型请求或增加实验容器。完整 bootstrap、16 次终态及用量核验仍待完成。

实际平台集成也仍有缺口：原生 `run_research_cycle` 的结果是写入想法队列后的接纳数量，它不执行这里的一次确认/一次审计流程。不能只改变原生模型参数就声称接入了被测确认策略；若候选通过后续完整门槛，仍须验证实际研究入口和验收绑定。

原始哈希、逐项测量及范围见 [首批闭合审阅](first-completed-review-001.json)。本次开发采用已观察任务，产品默认不变。
