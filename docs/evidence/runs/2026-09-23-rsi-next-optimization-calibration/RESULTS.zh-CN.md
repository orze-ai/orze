# 优化独立校准：参考可运行，2% 目标尚未取得合格见证

已按运行前冻结的 `plan.json` 完成 6 个问题/方法组合，使用各 32 个独立生成的校准实例。它们与后续训练、开发、确认及审计实例不相同；不是实际采集数据，也不是模型研究结果。

## 结果

| 问题 | 参考 v0 均值 | 参考 v1 均值 | 校准见证 | 判断 |
|---|---:|---:|---:|---|
| 128 点最大割，最小化未割边权 | 26567.71875 | 26525.46875 | 26268.25 | 未达 2% |
| 20 工件/15 机器调度，最小化 makespan | 1534.34375 | 1535.1875 | 首实例触发 15 秒安全上限 | 执行失败 |

最大割的正式校准比较对象是**逐实例取两个参考中更好的解**，均值 26463.59375；这比将来仅按开发均值选择一个参考更强。见证相对它改善 **0.7382%**，配对分组 bootstrap 差值的 97.5% 上界 -146.309375，虽有测得改善，仍未达到预定 2%。相对单独较好的 v1 也仅改善约 0.97%。不调整门槛或从任务池中删除这次失败。

最大割见证使用 16 个起点、每起点 8000 次禁忌搜索迭代及 aspiration；固定计算量，32 个实例独立重载预测耗时 5.34 秒。调度见证把同一 hinted CP-SAT 从参考的 0.5 确定性单位增加到 5 单位，但遇到预先设置的 15 秒单实例墙钟上限，按失败记录，没有用被截断的结果冒充确定性完成。两种调度参考的 32 实例重载约 121.58 / 119.58 秒，均完全复现。

见证是人工写的目标可达性检查，不会作为研究模型生成的方法插入对照。当前失败**不证明目标数学上不可能**；它证明这些具体见证还不足以支持启动正式优化研究对照。继续增加 CP-SAT 时间也不能忽略完整预测资源上限。

## 实现与核验

沿用已准备的四种共同参考，保持源码和资源不变。仅在新目录把 executor 指向已有固定求解器镜像 `sha256:8eb4246b6fe601f0e6894f5d8c882aea8b6f2ef7d7aceb892c11db993e2d9deb`；旧预检目录、镜像、服务及冻结实验均未修改。

- 4 个共同参考组合及最大割见证共 **5/6** 有效，均完成真实 worker 保存、另容器重载和宿主独立评分。
- 校准 **11 个容器全部闭合**，其中调度见证退出失败；10 份预测独立重算，5 组重载预测逐元素完全一致。
- 运行前还在独立 CPU 容器验证 9 个可穷举小图及 1 个小调度；10 个见证均达到对应小实例已知最优。小实例检查不能抵消大实例失败。
- `process-closed.json.exit_code=1` 明确保留校准失败；`verification.json.integrity_valid=true` 只表示记录、评分、源码冻结和进程闭合核验通过，`calibration_passed=false`。
- 没有挂载后续确认/审计数据，没有模型研究请求，没有新增 API 费用，没有最终研究验收成功。

下一步需要在相同资源边界内得到合格可达性证据，或在任何新研究候选请求前重新论证任务设计并保留本轮失败。公共数据集参考预检可独立推进，不能据它通过而跳过优化领域。
