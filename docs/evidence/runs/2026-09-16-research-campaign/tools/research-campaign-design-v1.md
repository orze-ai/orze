# Next required P3 work: execute and collect complete comparison runs

Previous goal turn was progress: lifecycle Core9594900 / Proc32c373 pushed,
clean and exact remote readback. No live runs. Current source baselines are those commits.
Original558f8c6 handoff reread in full; do not expand it into endless micro-optimization requirements.

Read current comparison protocol/reducer, scheduling task auditor, Pro usage auditor,
and actual scheduling Pro->CPU product driver. Gap is concrete: no complete runtime-bound
campaign executor/collector joins them. Current task auditor explicitly leaves campaign
identity false and sums inner controller wall time, excluding inter-call work/gaps.
P3 remains unproven while required costs unknown; do not change null to zero.

An async required-information question is pending for real model/call method and total
call/cost ceiling. It names the explicit user/plan restriction on real accounts/fees.
Do not repeat it. Continue isolated CPU/offline implementation. No subagents.

## Implement the execution boundary and actual scheduling workload together

Examples research_comparison campaign runner, worker bootstrap and scheduling workload:

- Freeze plan+slot and actual inputs, matching model/tools/environment, per-arm runtime
  package content and treatment, data/evaluator/instructions/initial state. Fresh projects
  only; refuse an existing output so unknown execution is not automatically repeated.
- Execute an actual owned workload process using existing prepare_supervised handles.
  Child checks its actual import roots/content before workload execution and after closure.
  Include harness/evaluator code in verifier identity, not in the A/B treatment artifact.
  Parent records intent before birth, full outer interval and actual closure; interrupted,
  failing and incomplete runs remain records with unknown fields, not removed runs.
- Fixed finite acceptance rounds/candidate limits are shared outer controls; leave product
  autonomous continue/wait/stop rules unchanged. Protocol seed controls explicit workload
  candidate ordering; disclose that provider sampling remains unseeded where unsupported.
- Scheduling workload calls real Pro research CLI (normal licensing), executes every accepted
  candidate through real native CPU CLI, evaluates candidate bytes, selects by independently
  recomputed valid score, and independently repeats the selected evaluator. Capture before
  first activity, all invocations and full refs, native prompt manifests and failed/retried
  usage, final DB/bytes. Provider request inventory must not come from surviving usage rows.
- Join raw task-quality and usage audits with runner-observed runtime/input/clock binding.
  Whole-project wall includes research and between-step work. Keep unavailable provider
  billing, GPU/worker CPU totals and policy consumption clocks unknown. Do not equate
  evaluator completion to evidence consumption. Required unknown costs still fail gates.
- Runtime manifest must derive from actual imported package roots with stdlib so pinned
  older artifacts need not already contain the new collector. Compare portable content
  identities separately from the exact local roots used for that run.
- Never log credentials; credentials enter child environment only. Fix research-affecting
  streaming/fallback/token settings explicitly. Initial implementation requires explicit
  empty history/memory; do not claim old-state restore from a fresh campaign.

## Evidence / tests

First record missing execution feature against current baseline. Product wiring uses owned
local HTTP substitute (normal HTTP stack) and real Pro/native CPU execution, including a
valid and invalid candidate, complete project wall vs inner sums, interrupted/missing calls,
replay/output collision, source/input/arm mismatch. A/A wiring controls are not A/B gains.
Preserve every executed version/failure. Full Core/Pro suites and paired31 after source freezes,
static independent product/accounting audit, ordinary paired commits/pushes as original plan.

Do not ask for real execution approval until a concrete protocol/task set/baseline/treatment
and expected quota are reviewable. Missing model constraints can be supplied asynchronously.
No actual model/account/GPU/existing-service use or production deployment is authorized now.

## Scope limits

This is the missing executing/collecting workload adapter, not a new research result.
No cold service recovery or storage redesign in this slice. Original CephFS/storage,
service migration/backup/rollback, real model/multi-round quality and controlled deployment
remain open. Do not call the original goal complete or blocked while meaningful local work remains.
