# 原生 sonnet 别名：本次成功调用标识为 Sonnet 5

在前一混合研究实验全部结束、核验和结算后，只运行了一次隔离目录中的 CLI 身份探测，提示仅为 `Reply with exactly OK.`。使用项目订阅代理同样的四项 Anthropic 环境变量清除规则，保留 `--model sonnet`；将输出改为 JSON，关闭工具、MCP、技能和 hooks，最多一轮。没有启动代理服务、修改原项目服务、发送新研究任务或读取新评测数据。

进程退出 0，结果为 OK，返回会话 ID 与本次显式会话 ID 相同。CLI 用量记录的主模型为 **claude-sonnet-5**，其输入、输出和缓存字段逐项匹配最终消息用量；另外记录了 **claude-haiku-4-5-20251001** 的辅助用量。不能忽略这部分辅助消耗，或把一次 CLI 调用说成恰好一次提供商请求。

CLI 报告的 API 标价估算共 $0.0242388。全部模型及缓存共 9,850 tokens，按原每百万 $25 规则保守保留 $0.246250；从 $5 预留单次释放 $4.753750。累计保守占用 $4625.724825 / $5000，剩余 $374.275175，历史 25 条未知仍保留。这是用量估算，不是订阅实际账单声明。

**改变下一步：原生配置的当前成功主模型是 Sonnet 5，不能把此前 Opus 5 控制组称为真实原生 incumbent。** 混合方案相对 Opus 5 的 42.88% 开发收益仍然保留，但后续必须将原生工作流及其 Sonnet 5 比较对象纳入验证。此探测只解析当前别名，没有验证历史运行、完整 research cycle、队列执行和最终交付能力，也不证明换成直接 Anthropic API 后延迟/行为等价。

探测的冻结计划、原始 CLI JSON、用量匹配和单次结算记录均保留。没有根据模型自述判断身份。
