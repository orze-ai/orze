# 已结束材料研究预算释放

原 `research-materials-20260918` 的 $224 池一直保留在后续累计账本中。本次核对从材料研究到 parent/context/long/combined/GPU/跨领域/开发候选及最新模型筛选的完整预算变化，确认没有重复释放。旧池永久退休，不得恢复调用。

28 次调用全部有完整用量，进程均已消失，调用起止、单次 attempt、模型身份和响应相符；574,682 个 token 按原最高 $50/M 保守保留 $28.7341，释放未用预留 $195.2659。没有改变单价假设，没有新增研究请求。

累计保守预留 $4807.64405 → **$4612.37815 / $5000**，尚未分配 **$387.62185**。正在运行的模型筛选 $135 池仍全额保留，25 条历史未知费用未释放。因而无需压缩每个研究的提案预算，已可支持后续预留 32×$10；该下一轮池尚未分配。

`budget-release.json` 是新的累计账本，包含各输入证据哈希。`reconcile.py` 核对调用与金额链，另逐项重算 token、保留金额和所有证据哈希。这里释放的是未用额度，不是研究能力收益或账户总消费声明。
