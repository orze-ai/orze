# 编译 GLS 校准仍未达到门槛

两批新 160 点 TSP 实例分别改善 **2.3520% / 2.3688%**，均未达到固定 2.5% 校准余量。所有 6 份预测重算、6 个容器闭合、128 个新实例互斥及冻结哈希核验通过，验证进程退出 0；失败是目标幅度不足，不是程序或核验失败。

完整 2-opt 循环换为镜像已有 Numba 编译实现，无 fastmath、暖缓存或并行搜索；2500 步小图路径、惩罚、增广成本及最优长度对照通过。首次检查因容器 root 身份无法在 FSx 写结果而退出 1，改为工作区用户后同一算法通过，记录保留。400 秒整批预算包括首次编译，两份原参考和 alpha 不变。

此次采用全新实例，不能与上一轮不同批次的 2.3536% / 2.4932% 作配对因果比较，也没有据此证明编译没有速度价值。明确结论只有：本次仍没有达到校准门槛，不能启动完整效能比较。

下一步不继续微调这些 160 点实例，而在首次未来研究请求前单独冻结 320 点规格的两批可达性校准。原起点/迭代强度、同一见证和 2.5% 门槛均保留；规格变化及所有历史失败完整披露。0 研究模型请求，人工见证不计研究能力或 30% 效率。
