# 编译固定 GLS 的可达性校准：算法和预算不变

此前 NumPy GLS 两批改善 2.3536% / 2.4932%，未通过原 2.5% 校准门槛。本次只减少完整 2-opt 搜索内层的解释器和矩阵分配开销，用同一固定镜像已有的 Numba 编译循环。距离、完整邻域、边惩罚效用、alpha=0.3、最近邻起点规则均保持；不开 fastmath，不用并行搜索或暖编译缓存。全部旧校准结果保留。

首轮编译计入原 400 秒整批预算；每 32 个搜索步骤检查时间，允许这个有限粒度的边界误差，容器仍限 600 秒。没有免费预编译。独立 4/7/12/24/40 节点小图逐步对比旧 NumPy 实现的路径、惩罚、增广成本和最优长度，共 2500 步；检查不使用校准或未来效能数据。

在两批全新 160 点均匀/聚簇各半实例上测试，各 64 例，原两个参考源码/模型/重启数/迭代数不变。参考每批最多 1800 秒；两个参考逐实例更优值作比较基准。两批均须至少 2.5% 均值改善且配对 bootstrap 差值 97.5% 上界小于零。未来效能目标仍为 2%；无论结果如何不改本门槛。

GPU 6/7 每批串行执行两个参考和一个见证，共六个离线容器，各 2 CPU / 12 GiB；旧试验与其他服务不动。输出按原宿主评分独立重算，检查冻结哈希、全部闭合以及与两轮旧校准和已准备效能输入互斥。失败原样保留，不在同一批上再调参。

这只是人工固定求解器提供的任务可达见证，0 研究模型请求，不是自主研究对照，不计入 30% 研究效率或能力收益。合格仍只支持该分布进入新输入预检，不保证每个未来随机批次可达；未来两组须先共同冻结任务与资源规则。
