# 下一轮隔离执行接口：仅完成宿主检查

复用此前共同参考的 worker、executor、metrics 和公共数据 baseline；只扩展最大割的顶点标签输出、调度的开始时间矩阵，以及宿主目标计算。独立容器前缀为 `orze-next-ref-`。没有改变当前冻结实验文件或产品默认。

宿主评分器从原始图/工时重算目标并验证解合法性，不采信候选分数。`check_adapter.py` 仅在独立校准池的三个实例/类上使用简单固定输出检查接口，不拟合或比较研究方法：6 份目标值与独立公式一致，13 个非法输入案例被拒绝，2 个构造的验收比较符合预期。没有在宿主执行任何候选源码或反序列化检查点。

当前 `adapter-verification.json.valid=true` 只覆盖宿主合同。隔离容器中的训练、模型保存、重载预测及挂载边界尚未进行端到端验证。新优化问题的竞争性共同参考和目标可达性也尚未完成；现有 baseline 只可复用于原公共任务接口，不能直接处理新优化问题。没有预检冻结计划，因此尚未启动下一轮预检或研究请求。

原始目录为 `/hot-data/fsx/workspace/erik/work/orze-p1-continuation-20260914/rsi-next-preflight-20260923`。Core 保存检查结果，Pro 同时保存源码；不保存数据、私有种子或检查点。新增 API 请求与 GPU 执行均为 0。
