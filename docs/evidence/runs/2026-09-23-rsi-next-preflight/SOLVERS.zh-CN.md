# 优化共同参考与独立求解器环境

准备完成，尚未执行完整校准或新研究对照。

- 最大割参考：128 次随机起点的单顶点改进；第二种参考从同类局部解出发用 CP-SAT 搜索。返回顶点分区，宿主重算未割边权。
- 调度参考：32 个确定种子的优先规则调度产生初解，CP-SAT 继续优化；两种参考使用不同确定种子。顺序、机器互斥及 makespan 建模对应 [OR-Tools 官方说明](https://developers.google.com/optimization/scheduling/job_shop)。参考也输出完整开始时间，宿主独立检查合法性及目标。
- CP-SAT 单线程、0.5 确定性搜索时间单位；20 秒墙钟只作安全上限，触发即失败，不静默改变共同参考的搜索量。0.5 确定性单位不等于 0.5 秒墙钟。两种参考将仅按开发集选择，未据未来研究候选表现调整。

新镜像 `sha256:8eb4246b6fe601f0e6894f5d8c882aea8b6f2ef7d7aceb892c11db993e2d9deb` 从原固定镜像派生，只新增 OR-Tools 9.15.6755 与 immutabledict，保留原 NumPy/Pandas/Protobuf/absl/typing-extensions 版本。软件包来自 PyPI 官方版本元数据并校验 SHA256；不安装到宿主，不改变旧镜像和正在运行的研究服务。第一次派生试构建发现会降级已有依赖，已改为保留原版本并生成独立的新镜像；试构建未用于研究。

独立 CPU 容器在 3 个可穷举小图、2 个可穷举小调度上检查两种参考，共 10 个方法/实例组合。CP-SAT 解均达到穷举最优值，各组合重复输出一致；容器退出 0 并闭合。检查只覆盖这些小实例，不证明大实例最优性、速度或 2% 改善可达。

下一步仍须在独立校准池运行参考和更强求解见证，在共同资源范围内验证目标空间；随后才能冻结真实参考。当前 executor 仍使用旧镜像，GPU worker 的训练/保存/独立重载尚未验证，因此不能启动下一轮研究对照。新增研究模型请求为 0。
