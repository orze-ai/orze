# 320 点 TSP 的独立可达性校准

160 点的原见证、NumPy GLS 和编译 GLS 均未通过 2.5% 校准余量；最新编译 GLS 两批仅改善 2.3520% / 2.3688%。保留所有负结果，不继续在这些实例上微调求解器或放宽门槛。

本次在任何新研究候选运行前，只检验一个新的任务规格：320 点 TSP，仍为均匀/聚簇各半，同一生成机制，两批全新实例，每批 64 个。维度提高可能增加组合搜索的改善空间，是待校准假设，不是已证实可达，也不是 orze 能力收益。不能把此规格与旧规格的分数直接作算法因果比较。

完整冻结后运行六次容器，GPU 6/7 各自串行。两份原参考的源码、24/48 个起点和每起点 300 次 2-opt 上限均保持；使用逐实例较优解作为参考包络，不因参考更慢或更强而删掉它。参考最多 1800 秒。固定编译 GLS 与上一轮逐字节相同，alpha=0.3，整批 400 秒包括首轮编译，容器最多 600 秒。沿用 2500 步独立小图等价性检查，无新增调参。

两批均须 >=2.5% 均值改善且配对 bootstrap 差值 97.5% 上界为负；任何超时、无效输出或未过门槛都保留为失败，不只挑有利实例。独立校验器核对全部排列目标、冻结文件及闭合。未来效能目标仍为 2%，双方优化任务上限相同为 1800 秒且均计入 7200 秒交付期限。

若此规格合格，未来效能输入仍须重新生成并完整预检共同参考，再冻结双方研究协议。该变化会明确改变未来任务范围，而不会改写既有 80/160 点结果。若失败则不能声称校准完成。所有实例为生成问题；零研究模型请求，人工见证不计入自主研究成功率。
