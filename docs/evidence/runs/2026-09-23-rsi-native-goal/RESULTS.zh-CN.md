# 八 GPU 原生独立验收闭环：固定样例核验通过

这次验证运行入口、独立验收和生命周期，科学研究成功数为0，API请求为0，不能计入30%研究效率目标。Slurm1325555及8个step退出0；8张不同物理GPU、16个不重叠逻辑CPU、每step12GiB内存。8个原生方法COMPLETE，24个隔离worker闭合且预测独立重算一致；12份固定本地响应模拟两组传输。

Core修复Slurm内各step都显示GPU0导致不同物理设备争抢同一锁的问题：仅当驱动minor与Slurm整数分配一致时使用原有物理设备锁。不改变独占检查、继承锁或宿主控制器冲突规则；未知/不一致设备映射拒绝。GPU租约、受监督探测、产物发布相关59项检查通过。

适配器保留模型源码和检查点哈希，训练后在隔离进程独立重载确认/审计，宿主重算指标；确认最多一次，仅反馈通过状态，最终审计最多一次。候选不接触最终标签。该适配器以及预算/传输共11项离线检查通过；原生角色和执行器已有能力复用。

## 保留的失败与限制

- 首轮1325552：7个step因GPU0锁冲突退出，唯一启动的step又因只读根目录缺少新挂载目标而失败。没有科学验收。
- 第二轮1325554：设备和挂载修复后，8个固定样例虽完成确认/审计，但源码硬链接被原生产物完整性检查拒绝，8个外层step失败。不能只看goal-delivered就宣称闭环通过。
- 第三轮改用独立源码副本，保留单链接产物检查，8/8完整通过。部分已入队提案在交付后被适配器拒绝，没有再次执行GPU；后续公共适配器在终态同时设置原生launcher暂停标志，避免这段清理损耗。
- 资源为A10040GB，PyTorch上限6GiB；swap/pids未新增独立上限。固定常数回归样例只验证接口，真实四领域收益仍待32次前瞻研究。

当前累计保守API占用$4629.212175，未分配$370.787825；本轮无新增费用。此前25条未知费用仍保留。所有旧实验保持闭合。
