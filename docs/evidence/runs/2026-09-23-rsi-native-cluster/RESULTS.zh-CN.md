# 原生 GPU 执行与反馈闭环已验证，30% 目标仍待前瞻研究

## 已完成的关键一步

独占 Slurm 分配的 a100-42 空闲 A100 上，公开持续 CLI 经过真实研究角色、三个提案入队、原生 GPU 所有权检查和调度、隔离执行、独立重载评分、结果记账及下一轮提示。修正后的三个提案均为原生 `COMPLETE`，六个 worker 正常退出，三个训练进程树均有 `TREE_CLOSED / ECHILD_WALL` 证明。控制器退出 0，下一轮实际 HTTP 请求包含三个方法的开发分数。

所有响应都是明确标记 `fixture-only:sonnet` 的本地固定响应，使用已经观察过的 TREC 和共同参考。开发错误率均为 126/817 = 15.4222766%，没有研究改善，也没有最终验收。每轮三个提案、900 秒 cooldown 保留；显式触发及固定响应不验证付费模型、自然触发节奏或完整 incumbent 研究效率。多个研究角色与 ParallelRefine 的分支语义仍须区分。

## 保留的失败与独立复核

1. 首轮分配 1325542：六次隔离执行均正常，适配器写小写 `completed`；原生接口只接纳 `COMPLETED/FAILED`，三个结果因此被正确拒绝。错误发生在本次适配器，无需改变产品验收规则。
2. 新目录第二轮分配 1325543：适配器改为 `COMPLETED` 后，三个任务完成，所有 worker 和控制器退出 0；外层验证脚本误将数据库终态断言为 `COMPLETED`，实际协议为 `COMPLETE`，导致驱动和 Slurm 作业退出 1。这个失败状态及原脚本完整保留。
3. `verify_workflow.py` 只读取原始记录：重新评分两轮全部 12 份预测，核对提示哈希、4 次本地响应身份、角色结算、三个原生完成状态、进程树、源码与原计划哈希。`workflow-verification.json` 通过。这修正了验证脚本的判断，没有修改两轮失败记录或重新执行候选。
4. 独立核验脚本最初假设失败流程也创建 `research_artifacts` 表；原生拒绝发生在发布之前，表不存在。改为仅导出实际存在的可选表，必需的生命周期表和所有验收断言仍然检查；第一次只读检查没有生成成功报告。

## 资源与隔离证据

本机八张卡有外部计算，保持所有权检查。Slurm 能分配独占空闲节点。远端 Docker socket 没有用户权限，使用节点已有 Enroot/Pyxis，从原不可变镜像 `sha256:8eb4246b6fe601f0e6894f5d8c882aea8b6f2ef7d7aceb892c11db993e2d9deb` 导出；镜像与导出文件哈希见 `image-export.json`。没有修改服务、用户组或系统权限。

最终 worker 验证了：私有 PID 和网络命名空间、只见 PID 1 和 loopback、宿主工作目录与凭证不可见、能力集为零、禁止提权、根文件系统只读、实际 GPU 矩阵运算正确。生成代码和序列化对象只在 worker 中执行/加载；宿主只处理数值预测。

初始化失败包括 Slurm prolog/通信故障、容器内重挂 `/proc` 被拒、缺少 squashfuse、挂载目标/字段格式错误、镜像 Python ENTRYPOINT 误读 ELF，以及集群默认启用可写根文件系统。最终采用先建宿主私有命名空间、再启动已解包的 Enroot 根目录、显式 entrypoint 和只读设置。所有失败记录保留；超时后依据作业终态或缺失句柄确认关闭才再次申请资源。

资源核查分配 1325544 证明祖先 cgroup 的内存硬上限是 12 GiB，CPU 集为逻辑核 0、64；最初只检查 cgroup 根目录未找到限制，后续沿真实进程 cgroup 祖先读取确认。swap 和进程数上限均为 `max`。这台 GPU 是 **40 GB A100**，不同于此前 80 GB；本次 `.075` PyTorch 显存比例约为 3 GB。后续前瞻试验须共同冻结实际资源规则，不能声称已经与旧 Docker 的所有限制完全相同。

远端缺少控制器依赖，系统 Python 无 ensurepip；已在工作目录通过独立 `pip --target` 安装依赖，版本见两个 workflow 目录的 `dependencies.json`。没有修改系统包。远端尚无 Claude CLI 或登录凭证；真实 incumbent 提供方接入仍待完成。

## 下一步与边界

原生 GPU 调度的资源阻塞已解除。下一步把研究候选落实为实际入口可执行的策略，接入真实 incumbent 提供方和共同独立验收，再冻结八任务、两重复、两组共 32 次研究。若原生策略定义与开发筛选不同，须明确登记为新候选，不能继承旧 42.88% 的成绩。

十一篇 RSI 原文及采用/未采用理由仍见两仓 `docs/plans/2026-09-22-rsi-efficiency-30.zh-CN.md`。本次继续遵循小改动、完整失败记录、真实运行时间和新任务迁移原则，不新增评审层。30% 时间改善、四领域配对区间、至少三领域更快、各领域成功率不下降及完整结算要求均不变。

本次新增研究模型请求 0、API 费用 $0。账本仍为累计保守占用 $4627.321975、未分配 $372.678025，历史未知费用保持保留。科学最终验收 0，产品默认未升级，目标未完成。`owned-jobs-closed.json` 记录本轮命名作业均已终止；未创建任务或监控。
