# 释放已结束 parent 试验的多余预留

沿每一笔后续账本转换核对，2026-09-18 的 parent 试验 $280 池一直完整留在累计预留中，未被后来的其他池释放重复扣减。

重新读取 28 次原始调用记录：27 次完整已知用量合计 631,512 tokens，仍使用原协议较保守的 **$50/M 全 token 上限**，保留 $31.5756；另外一次未知用量调用保留其所在 invocation 的完整原上限 **$10**。所有原请求进程均已不存在，已知调用的开始、结束、模型身份、token 组成及对应返回文件一致。旧试验保持退休，不再恢复。

因此保留 $41.5756，释放未使用的 $238.4244。累计保守预留由 $4911.06845 降至 **$4672.64405 / $5000**，剩余授权 **$327.35595**。全部历史 25 条未知费用继续覆盖；没有更改价格假设、没有释放未知费用、没有新增模型请求。

`reconcile.py` 检查连续账本转换及原始记录；`budget-release.json` 保存 71 个证据文件哈希、逐调用保留额及前账本哈希。已另行读取全部证据重核哈希与加总。剩余额度可支持下一轮 32 次研究各 $10 的对等预算，从而减少原 $5 上限下未准入提案对探索的限制；实际新池须在新协议冻结时单独预留，当前尚未分配。
