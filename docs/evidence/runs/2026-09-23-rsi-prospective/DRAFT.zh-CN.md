# 下一轮跨领域研究对照：准备中，未冻结或启动

计划比较现部署 Opus 5 和开发筛选后选定的一个研究模型，只有模型不同；两边保持 ParallelRefine、相同提示、数据、参考、验收和资源。模型候选必须在九次开发对照结束并核验后选定，尚未选择。没有运行研究候选，没有分配新 API 池，没有创建新监控。

六个新真实数据集的 12 组已核验参考已绑定，预测和检查点保持原样，不重训或按最终结果重选。QAP 新实例共同参考已独立启动；TSP 仍需合格校准和新实例预检。全轮必须保留四领域八任务、每任务两次重复共 32 个 episode；不能先启动容易部分，再依据结果挑剩余任务。

复用已有执行器和独立验收流程，16 项离线检查通过，包括审计拒绝不能算开发成功、一次确认/一次最终、交付早于请求结算、未准入不发请求、错误模型不能结算、冻结前不能启动，以及任务资源在双方提示和实际执行中一致。没有新增研究策略或额外模型评审。

真实数据训练/分析 900 秒、整批预测 600 秒；优化任务两项均 1800 秒，与新共同参考预检一致。全部纳入原 7200 秒研究交付期限，失败仍计 7200 秒。现有镜像内 Numba 0.66.0 作为可用库向两边同时明确，编译也计时；不把人工校准求解器或代码给研究模型。

主目标和默认门槛保持：四领域等权平均验收达标时间至少降低 30%，各领域成功率不下降，配对领域时间差双侧 95% t 区间上界小于零，至少三个领域更快，完整独立核验及用量闭合。即使通过，也只支持本覆盖范围的默认决策，不能宣称所有科研任务或普遍创造力。

待完成：完整开发模型结果与方法审阅；TSP 合格校准；两个新优化任务共同参考；最终候选和完整协议冻结；从最新累计账本预留 32×$10。`plan.json` 尚不存在，实际启动会拒绝未冻结方案。`readiness-check.json` 只证明准备完整性，不是研究能力证据。

参考检查点合计约 4.87 GiB。执行草稿在每次提案前完整重算共享代码与当前任务输入/参考的字节哈希，启动、结束及独立校验仍重算全部任务；避免文本或图像任务每次重读两个约 2.3 GiB 的 SARCOS 森林。曾尝试的元数据缓存未通过“同大小写入并恢复 mtime”检查，已完全移除，未用于研究。独立校验器检查每任务的数据、源码、参考和检查点完整覆盖于相应哈希清单。这项共同验证开销简化不算模型研究能力收益。
