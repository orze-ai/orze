# 首批已结束实例的审阅（整轮仍在运行）

初始快照审阅前两条已结束实例，后续成对结果追加在下文。不比较两组总体均值，也不改变冻结实验或研究模型上下文。完整 32 次独立核验尚未发生。

- Abalone-0 control：八折按组交叉验证、物理派生特征、多模型非负组合。开发改善 11.18%、确认 20.03%，最终审计改善 9.79%，低于固定 10% 门槛；虽然配对差上界为负，仍不达标并计 7200 秒。实际流程约 199 秒。这个差异本身不能区分过拟合、抽样差异或分布变化。
- CIFAR-100-0 control：修改逐样本增强、网络/训练方案并组合三个成员，最终审计错误率 42.18%→35.74%，改善 15.27%，458.46 秒达标。多项共同改动不能分别归因；模型自述的单模型内部验证和乐观的组合验证要分开。
- 初始审阅时另见一次 AG News control 的 liblinear 多类别实现错误，属于执行失败。它不等价于科学假设被否定。

`review-001.json` 中 rationale 是模型源码自述，findings 是容器测量记录；不将它们直接当成机制已证实。`point-loss-check-001.json` 用独立 NumPy 算术、未导入控制器评分函数，重算两条审计点估计并核对目标门槛。该检查没有重做 bootstrap 区间，也不代替整轮独立验证。

付费未用提案也可能发生在终态审计失败之后；底层字段 unused_after_delivery 不代表交付成功。下一步仍按原协议完成所有实例及请求结算，再判断均值和默认。

## 组件耗时字段的含义

CIFAR-100 control 的 per_model[*].seconds 是源码中分配给各成员的预算（226.6/290.4/486.2 秒），不是实际耗时，不能相加。实际 train 自报 293.8 秒，worker 记录 294.14 秒，宿主容器记录 297.43 秒。主达标时间一直使用宿主起止事件，不依赖该字段；此次发现不改变验收或时间结果。详见 `timing-field-audit-001.json`。

## 首个完整成对结果：CIFAR-100 第一次重复

两组均通过最终验收：control 7.641 分钟，Opus 5.5 12.689 分钟；对应审计错误率 35.74% / 35.52%。后者实际加载父模型、只训练一个更宽模型，却按时间训练至 1083 epoch，循环约 669 秒；control 三个 240 epoch 成员合计实际训练约 294 秒。父模型复用和较少新模型数都不能替代达标时间测量。这里未分离架构、训练日程、响应等待和共享硬件影响，不做机制因果归因。

第二次重复已交换 GPU 0/1 继续；此单对结果不能推断领域或整轮均值。详见 `cifar100-pair-review-001.json`。原前两条实例审阅快照保持不变。

## 第二批审阅：正反结果和失败修复

本次新增审阅六个已经结算的实例，记录在 `review-002.json`。`point-loss-check-002.json` 独立重算其 17 份开发、确认和审计预测的点损失；未执行候选源码、未加载检查点、未发模型请求，也未占用 GPU。分类和回归用独立 NumPy 算术，TSP 用排列合法性检查及 Python `math.hypot`/`math.fsum` 计算闭合路线长度。所有点估计与宿主记录一致；bootstrap 和整轮完整性仍须等终态验证，不能将此局部检查当作整轮通过。

### CIFAR-100：第二次重复反向，合并后候选并未更快

第二次重复两组均达标，control 8.163 分钟、candidate 4.156 分钟；审计错误率分别为 35.04% / 34.96%。候选实际复用父模型，新网络训练 200 epochs、170.6 秒；control 训练两个各 200 epochs 的 ResNet-18，总训练 347.8 秒。第二次候选与第一次候选都复用父模型，但第一次训练到 1083 epochs，因此不能把“复用”本身解释为稳定的效率增益。

该任务两次重复全部结束：control 平均 **7.902 分钟**，candidate **8.422 分钟**，均成功 2/2。单次重复的胜负会反向；这也不是完整视觉领域或四领域的估计。control 第二次的 `member_info[*].seconds` 为从整个训练开始计算的累计时间（180.0 / 347.8 秒），不能相加；第一次的同类字段则是分配预算。统一采用宿主交付时间，组件耗时必须检查代码语义。

### AG News 首次重复：实现更顺利，也可能没有解决目标

两组均用完六次提案，最终均失败并计 120 分钟。control 最终审计错误率 10.36%→9.40%，改善 **9.27%**；candidate 为 10.36%→9.66%，改善 **6.76%**，均低于固定 10% 门槛。

候选第一步的开发改善为 11.56%，确认改善只有 0.53%，随后两条路径增加稀疏 MLP、更多表示/种子及嵌套按组验证，但五次后续提案没有超过第一步的开发成绩。最终仍选择第一步；更复杂的方法和更规范的内部估计尚未转化为这个实例的验收成功。

两组初步方法都组合词/字符/NB 特征及线性模型，权重通过按组交叉验证选择。control 在一个内部留出折上探测自训练，之后增加轮数反而使开发错误率由 9.10% 升到 9.55%。这个局部探测不是可迁移增益证明。两组均允许利用未标注特征，内部词表使用训练和开发文本；不将这些内部估计冒充最终审计。

control 外层记录两次无效尝试：多类别 `liblinear` 错误，以及源码为空或超长。candidate 外层无效次数为零，但第一步的 `lr_comb` 被内部跳过；源码同样使用多类别 `liblinear` 并捕获异常，未保留异常细节。结合 control 的同环境错误，接口不兼容是合理解释，不能当成已独立复现的根因。外层零无效不代表内部所有模型都成功。

### TSP-320 首次候选：实际改变搜索邻域并通过目标

candidate 使用 Numba 编译的 2-opt/Or-opt 迭代局部搜索、近邻候选列表和片段扰动；源码不读取标签或行位置先验，也不访问人工校准求解器。训练阶段只测四个训练实例，没有拟合数据驱动模型。训练报告比较的是最近邻路线，不能替代与完整多起点参考的比较。

最终宿主审计的 64 个新生成实例上，路线长度均值从 **10.962588 降至 10.607724**，改善 **3.2371%**，配对差上界为负；在 **23.905 分钟**完成验收。独立重算检查了每条路线的完整排列和闭环长度。训练及开发预测 worker 耗时 329.25 秒，确认 323.21 秒，审计 643.22 秒；约 9 秒的 `train()` 自报不包含后续开发求解，不能拿来称研究只花了 9 秒。

这是一个已测得的方法选择成功案例，使用的是现有启发式方法；不能据此声称新算法、优化领域平均胜出或通用创造力提升。对照和剩余优化实例仍按冻结协议运行。

### Abalone 首次候选：修复超时后达标，损耗仍计入时间

candidate 第二步移除高斯过程、调整模型顺序，并在每个交叉验证折之前检查预算；实际采用 SVR、树模型、MLP 和多项式回归的非负组合。新训练约 36.7 秒，最终 MSE 4.956797→4.436392，改善 **10.50%**，20.264 分钟交付。此前失败容器占用 **900.31 秒**，没有从研究时间中扣除；旧步骤反馈的 946.22 秒包含更广的调用耗时，不能当作训练耗时。

程序中确有代价高的高斯过程和仅在模型之间检查时间的旧实现，但没有保留逐阶段终止位置，不能确定它就是唯一超时原因。新方案通过与移除它同时发生，不构成单因素消融。预算检查仍发生在拟合之前，不能保证任意一次阻塞拟合都不超时。

## KMNIST 首次成对结果：减少计算后仍达到共同目标

control 9.028 分钟、candidate 3.326 分钟，两组均通过最终验收。最终错误率分别为 1.32% / 1.54%，共同参考为 2.32%。候选的错误率稍高，但均超过相对下降 10% 的固定门槛；此次比较按用户要求优先看达到同一目标的时间。

control 新训练八个各 110 epochs 的网络，实际训练 354.0 秒；预测时每个成员计算九个几何视图。candidate 实际加载父模型，训练四个各 60 epochs 的新网络，训练 129.4 秒；预测平均父模型和新模型共五个单视图输出。没有单独消融，不能把全部时间差归因于某一项变化。源码检查训练变换和最终预测均未使用水平翻转，符合 KMNIST 的语义约束。

两者报告的训练集准确率 100% 都是样本内测量，候选的开发预测分歧也是无标签诊断，不能算独立验证。`kmnist-pair-review-001.json` 独立重算两组共六份开发/确认/审计预测的分类点损失，均与宿主记录一致；未重做 bootstrap 或整轮核验。这里只报告第一次重复，不据此宣布该任务、视觉领域或整轮胜负。

## 当前可用的研究经验

- 以独立最终验收和完整墙钟时间判断方法是否有效；保留开发领先但最终失败的反例。
- 每项经验应附适用任务、实测结果和未分离的解释。父模型复用、更多模型、内部交叉验证和复杂推理均不能单独保证研究更快。
- 失败修复应区分实现问题与科学假设；记录实际损耗，不将捕获异常或跳过成员误算为能力提高。
- 当前材料没有注入在途研究的上下文；冻结设计、预算、参考和产品默认保持原样。下一决策仍取决于完整 32 次及预注册门槛。


## 第三批审阅：回归完整配对与 AG News 第二次重复

[review-003.json](review-003.json) 保存八个已结束实例的选择源码、模型原始理由、实际训练发现、执行时间、最终比较和证据哈希。[point-loss-check-003.json](point-loss-check-003.json) 对 22 份已保存预测独立重算分类错误率或 MSE，全部一致；检查代码为 `review_completed_003.py`，运行于分析目录，未导入研究候选或原评分模块，0 模型请求、0 GPU 执行。这里没有重新核验 bootstrap，也没有代替完整 32 次的最终核验。

| 已完整结束的任务/领域 | control 成功 | candidate 成功 | control 计分分钟 | candidate 计分分钟 |
|---|---:|---:|---:|---:|
| Abalone | 1/2 | 1/2 | 61.416 | 70.132 |
| SARCOS | 2/2 | 2/2 | 4.199 | 2.581 |
| 回归领域，任务等权 | 3/4 | 3/4 | 32.808 | 36.356 |
| AG News | 0/2 | 0/2 | 120.000 | 120.000 |

失败仍计原 120 分钟。SARCOS 两次均更快，不能抵消 Abalone 的长探索和失败；回归领域整体更慢。这是完整回归领域的局部结果，不是四领域整体结论；AG News 也不能代表仍在途的整个文本领域。

### SARCOS：双方都提出合理方法，候选实现更快但误差略大

四条选择源码都从树参考转向 `raw21 + sin/cos(前七个位置)` 的 MLP 集成，并使用训练组内部留出验证。候选实际以 `baddbmm`/`bmm` 批量训练八成员，control 顺序训练八/十成员；候选另在内部留出上选择 HGB/ExtraTrees 混合权重。训练自报耗时两次为 control 139.3/173.5 秒、candidate 63.1/85.0 秒；最终交付均值为 4.199/2.581 分钟。

外部审计 MSE 两次 control 为 4.3914/3.8386，candidate 为 4.4930/4.6029；四次都满足原改善门槛，但候选预测误差更大。不能把更快解释为所有质量指标都更好，也不能把基于刚体结构的常规方法称为新算法或只有候选具备的能力。没有单因素消融，步数、优化器、集成规模、混合方法和 GPU 分配差异仍在，不能将全部时间差因果归给批量矩阵乘法。内部留出也用于混合选择，不能将其当独立外部确认。

### Abalone：修剪昂贵部件不保证研究更快达标

第二次 control 在 2.833 分钟通过，审计 MSE 改善 11.038%；候选实际探索 20.572 分钟后，最终选择的 b1-s1 只改善 9.028%，按原 10% 门槛失败、计 120 分钟。候选源码删除 GP 并调整多个成员，实际训练 74 秒，但整个研究目标未完成；不将训练阶段简化当研究效率收益，也不把失败单独归因于删除 GP。

两边源码中所谓 nested CV 都先在所有训练行构造一次 OOF 矩阵，再仅交叉拟合 NNLS 混合权重：control b1-s0 第 174–194 行，candidate b1-s1 第 117–140 行。对某外层折 F，其他折的 OOF 预测来自曾训练过 F 标签的基学习器，再用于拟合混合权重，因此这不是整条方法完全隔离的外层验证。这个判断来自源码依赖关系，不是对偏差大小的实测，更不能据此否定独立外部审计或改写原验收。control 的“honest nested”原文保留并注明限制；candidate 也已自述不独立。完整嵌套验证应在外层训练部分内重新执行整个拟合/选择流程，参见 [scikit-learn 官方说明](https://scikit-learn.org/stable/auto_examples/model_selection/plot_nested_cross_validation_iris.html)。这里没有强制加入昂贵嵌套训练；是否值得增加诊断成本需要独立验证。

### AG News：内部探测有价值，但仍未跨过外部目标

第二次 control 的组 OOF 发现自训练错误率 0.09492 高于原组合 0.09408，实际关闭自训练；候选用组 OOF 选择组合权重与自训练，内部错误率从九成员等权 0.09567 降到 0.091。最终审计相对改善分别仅 6.375% / 9.562%，两者仍低于固定 10%。候选选中方法的训练 340.4 秒，包含约 194 秒自训练交叉验证；更多诊断和更好内部指标未转化成最终验收。原失败全部保留，不把接近门槛四舍五入为成功。


## 一次性确认触发的诊断：不增加未经证明有效的置信门槛

[confirmation-gate-diagnostic-001.json](confirmation-gate-diagnostic-001.json) 是观察到失败后提出的探索性诊断，使用快照中 23 个已结束实例、22 次已有确认。候选规则是在现有开发点估计改善门槛上，再要求开发集配对组 bootstrap 的 97.5% 差值上界小于零；5000 次重采样、seed 20260922，按原分组顺序计算。使用保存预测及独立标签/参考损失，0 新模型请求、0 新 GPU 执行，没有更改在途评估。

| 额外开发置信门槛的动作 | 已有确认通过 | 已有确认失败 |
|---|---:|---:|
| 允许确认 | 13 | 5 |
| 暂缓确认 | 2 | 2 |

被暂缓的两次实际通过是 KMNIST 第一次重复的两组；两次被挡下的失败是 TREC 第一次重复的两组。它仍放行 Abalone-1 candidate、AG News 三次和 KMNIST-1 candidate 的失败确认，**未击中本次 KMNIST 延迟的触发点**。这不是执行新规则后的结果：后续确认对象、模型反馈、探索顺序和真实墙钟时间都可能变化，不能从该表算出新成功率或节省时间。当前证据不足以增加该默认门槛，暂不实现；原确认/审计规则不变。

KMNIST-1 candidate 最终仍通过，交付 30.672 分钟，control 为 7.253 分钟。早期 b0-s0 确认错误率从参考 0.009 降到 0.006，但差值上界恰为零，按原严格规则未通过；预算末对开发最佳 b1-s0 审计，错误率 0.0118 对参考 0.0188，改善 37.234%、上界 -0.0042，才交付成功。不能将较早的开发集高分当早期验收完成。

两项完整领域现已固定为：视觉 control/candidate 平均 8.021/12.711 分钟，均 4/4 成功；回归 32.808/36.356 分钟，均 3/4 成功。**按现有闭合记录，本候选最多只能在剩余两个领域更快，已无法满足预注册“至少 3/4 领域更快”条件。** 这不等于整轮已结束或已经知道完整均值；仍须收齐全部 32 次、请求结算和独立核验，不停止、重跑或删除不利实例。

这项取舍对应 [RSIAgent §4.6](https://arxiv.org/html/2609.15364v1) 的限制：新增探索应命中实际未解决的决策，局部验证也不能代替完整要求。论文并未验证上述开发置信门槛；规则来自本地待检验假设，其负面诊断同样保留，不将其固化为有效研究经验。


## TREC 首次配对与 KMNIST 第二次配对：方法拓展和交付时机

[paired-text-vision-review-001.json](paired-text-vision-review-001.json) 保存四个已结束实例的实际选择方法、模型自述理由、训练测量、所有执行过的开发/确认/审计结果及证据哈希。27 份分类预测独立按标签重算错误数，全部一致；bootstrap 数值保留原记录，此处没有重跑完整核验、GPU 或研究模型，也没有加载候选 checkpoint。

**TREC 首次 candidate 是方法家族拓展，仍不是新算法证明。** 它在 b0-s0 稀疏方法确认失败后，b0-s1 实际增加从零训练的子词双向 GRU，并扩展问题开头/中心词结构特征；源码实现、CV 测量、最终五个神经网络的保存与 predict 中非零混合权重均已核对。最终混合权重为 SVC 0.5、LR 0、NN 1.0；NN 是最终预测实际使用的组成部分。内部神经网络 OOF 错误率 0.14047、SVC 0.13994、所选混合 0.13576，权重使用同一组 OOF 分数选择，仍有选择偏差；NN 内部三种种子与最终五种种子的区别也保留。

最终独立审计 489 行中，参考错误 60 个，control 最佳方法 52 个，candidate 37 个。control 改善 13.333% 但配对差值上界 +0.006135，未达标；candidate 改善 38.333%、上界 -0.022495，通过。实际候选训练 246.6 秒，但完整交付为 23.929 分钟；control 实际结束 13.748 分钟，失败计 120 分钟。结构特征和混合同时改变，没有拆分消融，不能把所有收益单独归给 GRU；两边分别运行六次调用，也不能把一条成功轨迹提升为 TREC 两次重复或全部文本的结论。

**KMNIST 第二次候选训练更短，完整研究却更慢。** control 训练八个新成员，训练 298.2 秒；candidate 确实加载参考，新增三个成员，训练 135.9 秒。最终审计错误率 control 0.0108、candidate 0.0118，相对共同参考 0.0188 均通过。模型复用没有保证本次更快交付：完整时间为 control 7.253、candidate 30.672 分钟，两次重复的任务均值为 8.141/16.999 分钟。

候选最终选择 b1-s0 在启动后 341.002 秒就已完成开发执行，最终交付距离该时刻仍有 1499.308 秒。早期 b0-s0 已消耗一次确认但未通过，后续按冻结规则继续六次尝试，最终在预算末审计开发最佳 b1-s0。期间 b1-s1/b1-s2 和 b0 后续版本都未改进这个开发最佳。这个间隔含后续探索及最终审计，**不能直接算作可节省的 25 分钟**：若改为提前选择/确认，其他未执行的确认结果和后续轨迹未知，而且 TREC 的成功确实依赖第一批之后的方法改变。此前额外开发置信门槛的诊断也没有拦住该次失败确认。

下一步应对具体的确认/选择时机和探索收益做有对照的检验，同时计入过早终止造成的失败；不能统一删掉后续研究或假设最佳方法在出现时就已独立验收。运行中的冻结代码、确认次数、任务阈值和默认配置保持原样。


## TSP 首次完整配对、TREC 第二次候选及源码上限

[tsp-trec-review-001.json](tsp-trec-review-001.json) 新增 TSP-0 control 与 TREC-1 candidate 的源码/测量审阅，九份预测独立重算一致。TSP 用 Python `math.hypot/fsum` 检查排列和闭环欧氏长度，TREC 按独立标签重算错误率；没有执行候选、重跑 bootstrap 或修改冻结实验。

### TSP：充分求解与固定批次预算的代价

首次两边均通过，control 71.629 分钟、candidate 23.905 分钟。control 审计路线均长 10.604873、candidate 10.607724，共同参考 10.962588；改善分别为 3.2631%/3.2371%，均超过原 2% 门槛。两者均是标准 2-opt/Or-opt 迭代局部搜索，不能把后者时间优势等同于发现新算法。

实际 control 模型设 `budget=1330`，在 `predict` 中顺序解各实例，按剩余批次时间平均分配、每实例上限 45 秒；开发/确认/审计实际预测接近各 1330 秒。candidate 使用两线程、每实例约 20 秒，32 实例批次约 320 秒、64 实例审计约 640 秒。两边近邻列表、扰动及局部搜索实现仍不同，不能将全部差异单因素归给线程数或预算。control 自报 `train_seconds=30.0` 仅指编译/四例训练探测；同一训练容器还执行开发预测，实际 worker 1360.3 秒。四例探测比较的是自身初始局部解，不是完整共同参考，不能当最终改善证明。

### TREC 第二次 candidate：把语义先验实现为可测特征

选中 b1-s1 将 city/author/year 等通用词、问题结构与短语线索编码为第四组特征，与词/字符/位置 TF-IDF 拼接；最终由 SVM 学习与投票预测。源码逐条检查了词典构造、特征变换和实际非零权重，它没有按测试行号给标签。这是模型已有语言知识的特征化，不应宣称新知识发现，也不能证明基础模型从未见过公开 TREC。

训练组 CV 比较了包含零词典权重的网格；自报无词典最佳错误率 0.1512、含词典最佳 0.1219、最终五成员 0.1208。但 144 组配置和集成大小仍在同一 OOF 分数上选择，不能当独立消融或无偏增益。独立审计 489 行中错误 50→29，改善 42.0%、差值上界 -0.02045，通过；完整交付 10.684 分钟。该报告尚不包含第二次 control，因此不在此宣布完整 TREC 配对结果。

### 源码格式限制：三个未执行的潜在方法

[source-limit-diagnostic-001.json](source-limit-diagnostic-001.json) 核对了已结束实例中的三次源码大小拒绝：AG News-0 control b1-s2 的实际源码 17463 字节；TREC-1 candidate b0-s2/b1-s2 为 17203/18525 字节。提供方均完整返回，原 16000 字节检查拒绝；只将静态解析参数改为 32000 时，三份均通过 Python 语法和 build/train/predict 入口检查。没有运行提案，没有改变任何限制，不能将三份算作已执行、有效科学实验或成功。

TREC 两次后续拒绝的完整原始回答与三份错误记录保留。它们会消耗付费请求及等待，完整有效性仍未知；单纯删除说明文字也不能保证都适配 16 KB，两个 TREC 源码的 docstring 内容分别只有约 1.2/1.1 KB。后续如检验更宽源码上限，可使用已有 parser 的 `max_bytes` 参数，在独立开发对照开始前同时固定提示、预算及准入规则，无需新增框架；必须连同更多执行、复杂代码风险和潜在收益一起测量，不能把语法通过当 30% 效率提升。

## TREC 完整配对：有探索，并不保证可验收收益

第二次 control 已闭合，六次提案均有效，但最终未通过。[完整文本配对审阅](trec-complete-review-001.json) 独立重算了它的全部八份预测；没有执行候选或重跑 bootstrap。选中 b1-s1 在 489 条最终审计中由参考的 50 个错误降为 47 个：修正 15 个，同时引入 12 个；相对改善仅 6%，低于 10%，配对差上界为正。实际研究结束用时 24.176 分钟，按冻结失败规则计 120 分钟。

control 同样进行了方法拓展：b0-s1 实际训练 word-CNN 做内部交叉验证，准确率 82.39% 低于稀疏集成 85.11%，最终将 CNN 权重选为零、没有训练最终 CNN；其开发预测与 b0-s0 逐项相同。`vocab_size=2` 是不采用 CNN 后的占位状态，不能误判为最初训练词表只有两个词。选中的 b1-s1 则实际保留三种子 MLP、NB-SVM 与其他成员，最终预测中权重非零。因此不能把本次 control 失败解释为“只会调参、从未尝试神经方法”。

b1-s1 也暴露了内部选择问题：单个最佳成员的 OOF 错误率为 0.15360，三个集成权重方案中最好的仍为 0.15963；源码只在三个混合方案中选，没有把最佳单成员作为同一选择集合的候选。它测到了反对“集成更好”的证据，仍输出集成。该诊断提示后续应检验方法选择是否响应负证据，不能直接假定改选单成员会提高独立审计或缩短达标时间；这需要新的实际对照，当前方法保持冻结。

| 完整文本任务 | control 通过 | candidate 通过 | control 计分均值 / 分钟 | candidate 计分均值 / 分钟 |
|---|---:|---:|---:|---:|
| TREC，两次重复 | 0/2 | 2/2 | 120.000 | 17.307 |
| AG News，两次重复 | 0/2 | 0/2 | 120.000 | 120.000 |
| 文本领域 | 0/4 | 2/4 | 120.000 | 68.653 |

candidate 的 TREC 两次成功分别来自子词 BiGRU 混合和语义词典特征，局部能力差异有实际方法与验收证据，但没有独立单因素消融。AG News 没有解决、视觉和回归领域均值更慢；增加方法名称、内部分数或提案数量都不是通用能力指标。完整 32 次及终态核验仍未完成，候选仍不能升级默认，30% 目标仍未达到。

## 是否值得先重写异步调度：按真实时间段检查

[等待消除诊断](wait-removal-diagnostic-001.json) 覆盖三个完整领域的 24 次研究，保存 356 个原始输入哈希。按模型回答的 started/finished 与容器 dispatch/closure 时间段求并集、交集，裁剪到最终交付；没有把 `outcome.seconds` 当模型延迟，也没有把 CPU 容器运行当 GPU 核心利用率。

当前 `run_prepared` 已并行准备、按完成顺序串行执行；仍有批次边界等待。本次检查先问一个更乐观、范围明确的问题：**保持原方法、成功/失败、确认与选择路径以及每次串行执行耗时，删除成功实例的全部非执行时间**，而失败仍计 120 分钟。它只是假设条件下的算术结果，不是实测新调度，也不保证可实现。

| 已完整领域 | control 原均值 / 分钟 | candidate 原均值 / 分钟 | candidate 删除全部非执行时间 / 分钟 |
|---|---:|---:|---:|
| 视觉 | 8.021 | 12.711 | 11.449 |
| 回归 | 32.808 | 36.356 | 35.415 |
| 文本 | 120.000 | 68.653 | 66.658 |

即使给 candidate 这一乐观条件，视觉、回归仍较慢，无法满足至少三个领域更快。例如 KMNIST-1 candidate 的 1840.31 秒交付中，1708.38 秒有执行容器，模型尚未返回且无执行容器的时间仅 128.58 秒；不能将整个长等待归因于模型响应或批次同步。

决定：不优先为“只消除当前轨迹空等”开发异步框架。下一候选应针对可验收方法质量、如何响应反证以及确认失败后的继续探索/交付决策提出可检验的改变，而不是先叠加调度工程。若改变执行顺序导致少做实验、并发执行、调整方法预算或改变成功率，就超出了上述条件，仍需在线对照，不能用这些数值预测收益。

重新核对 [SIFT §4.3](https://arxiv.org/html/2609.19526v1)：论文的主要加速来自异步展开与评估，评审引导的推测性展开另有贡献；后者会改变搜索路径。本地诊断不反驳 SIFT 或全部异步策略，只排除了把论文加速数字直接套到当前串行执行轨迹上的理由。优化领域尚未完成，未将其删出主试验，也未计算新的整轮均值。此次为只读分析，0 新模型请求、0 新执行，默认不变。

## 首批比较后再确认：可测假设及其代价

[初始确认选择诊断](initial-confirmation-choice-diagnostic-001.json) 固定分析已结束的前 26 个实例：若先评估首批两个分支，再将唯一一次确认用在开发最佳方法上，已观察的完整首批中有 4 次会换方法、4 次不变；另外 3 次本来就没有在首批使用确认。

会换方法的是 Abalone-1 candidate、KMNIST-1 candidate、TREC-0 control、TREC-1 candidate。这四次原确认均失败，但替代方法的确认没有执行过，不能推断改选即可通过。TREC-0 candidate 会保持同一首批选择，它最终依赖后续 BiGRU 方法才通过；因此该假设保留确认失败后的继续探索，不能变成第一批未通过就终止。

最重要的代价是另外 15 次在首批提前结束的研究：其中 14 次最终成功，第二份付费提案并未执行。只读解析确认 15 份完整源码均通过原 16 KB 语法/入口检查，均为 method，但运行时长、预测及最终有效性未知。延后确认会要求处理这些方法，可能增加执行与等待，也可能改变选择和成败；不能把“已付费”当成执行成本为零，或用模型自报预算代替实测。

这一假设比追加笼统置信门槛更直接针对已观察的确认时机问题，但证据尚不能决定平均收益。下一步只把它作为当前冻结试验关闭后的**小规模配对开发对照候选**，同时覆盖潜在获益与现有快速成功案例；开发筛选不代替新的跨领域前瞻验收。没有改变在途研究、最终审计次数或产品默认，也未发额外请求。

## TSP 第二次 candidate 闭合

[第二次候选源码与三份预测审阅](tsp-second-candidate-review-001.json)：候选在 33.575 分钟通过，64 个新生成审计实例的平均路线长度由参考 10.933520 降至 10.578088，改善 3.2508%，配对差上界 -0.33013。独立检查全部排列并以 `math.hypot/fsum` 重算开发、确认、审计路线长度一致；bootstrap 沿用原结果，没有重跑。

源码是 Numba 编译的 2-opt/Or-opt 迭代局部搜索、double-bridge 扰动和不劣接受，两个 CPU 线程成对处理实例，没有学习组件。每批预算 `min(900, 30*ceil(N/2))`，32 例开发/确认各约 480 秒，64 例审计约 900 秒；训练报告的 12 秒仅是两个训练实例相对单起点近邻的检查，实际训练加开发 worker 用时 492.04 秒。不能把短 train 字段当完整执行耗时，也不能把两次重复不同预算与不同实例当单因素加速对照。

当前总计 27/32 已结束、19 次通过；本次 control 和全部 QAP 尚未结束。TSP 第二次两组配对均值、完整优化领域和整轮均值仍待收齐。两次付费请求中一份未用，费用与原始输出保留。


## TSP 两次完整配对：更短预算下均通过，不能外推为整轮收益

[完整 TSP 审阅](tsp-complete-review-001.json) 补齐第二次 control，并独立检查其开发、确认、审计三份排列及路线长度；以 Python `math.hypot/fsum` 同时重算相应参考路线，均与宿主记录一致。未执行候选、加载检查点或重跑 bootstrap。前三个已结束实例的检查保留并按哈希关联。

| TSP-320，两次重复 | control | candidate |
|---|---:|---:|
| 最终验收通过 | 2/2 | 2/2 |
| 平均达标时间 / 分钟 | 72.617 | 28.740 |
| 付费提案 / 未使用 | 4 / 2 | 4 / 2 |

这一个生成问题类的样本均值下降 **60.4228%**。第二次 control 73.606 分钟交付，审计平均路线长度 10.567624，相对共同参考 10.933520 改善 3.3466%；candidate 为 33.575 分钟、10.578088、改善 3.2508%，两边均满足原门槛。候选更快但该次路线略长，不能把达标时间与最优路线质量混为一个指标。

control 实际使用 Numba 编译、K=10 邻域、2-opt/Or-opt、double-bridge 和严格改善接受，按实例顺序执行；每批预测预算固定 1400 秒，实际训练加开发 1421.45 秒、确认 1400.20 秒、审计 1400.20 秒。candidate 采用两个 CPU 线程、开发/确认各 480 秒和审计 900 秒。算法细节、线程及预算共同变化，没有单因素消融；这不是 GPU 核心加速或新算法证明。control 的 21.24 秒训练报告仅含编译和三个训练实例的短检查，比较的是单起点近邻，没有检验完整参考的局部最优性，也不能代表整个研究耗时。

四次研究各留下一份已付费未执行提案。下一开发候选若先测完两条分支再确认，必须实测这些原本可跳过的执行代价，不能把已付费当作零额外耗时。当前 28/32 闭合、20 次通过，QAP 两组按原计划运行；完整优化领域均值和整轮核验仍未完成。视觉、回归已完整且候选更慢，原至少三个领域更快的默认门槛不能达成，产品默认不变。


## QAP 首次 candidate 通过：常规禁忌搜索，配对尚未闭合

[首次 QAP 候选审阅](qap-first-candidate-review-001.json) 记录 candidate 在 **29.314 分钟**验收交付并结清两次请求，其中一份付费提案未使用（估算 $0.242664）。64 个生成审计实例的目标均值从固定参考 218.551647 降至 199.849531，改善 **8.5573%**，配对差值上界 -17.20964，通过原 2% 及不确定性门槛。全部排列有效，以 Python `math.fsum` 双重求和独立重算目标一致；没有执行候选、加载模型或重跑 bootstrap。

[先前闭合阶段重算](qap-first-stages-pointloss-001.json) 同时保存两组首份开发和候选确认的三份检查：开发均值 control 201.786939、candidate 200.641168，对应共同参考 221.019319；candidate 确认均值 204.783496，对应参考 224.559980，改善 8.8068%。这些开发/确认结果不另算最终成功。

两组首份源码都实现 Taillard 风格的编译禁忌搜索、交换差值表、随机 tabu tenure 与 aspiration，并都使用两个 CPU 线程。candidate 用两个释放 GIL 的 Numba worker，每实例预算上限 25 秒、整批 1000 秒，每个 30000 步阶段结束后才检查时间。实际训练加开发、确认、审计 worker 分别 **421.63 / 413.32 / 834.05 秒**；训练自报的 12.15 秒只含编译与两例短探测，没有与完整参考比较，也未证实说明中约每秒十万次移动的速度。常规搜索方法与计算预算的选择可以影响研究效率，但不能把名称、训练自报或源码理由当作新算法或通用能力证明。

当前总计 **29/32 闭合、21 次通过**；首次 control 仍在确认，第二次 QAP 两组尚未启动。不能据单臂结果推断 QAP 配对均值、完整优化领域或整轮 30% 收益。实验未重启或修改，产品默认不变。


## QAP 首次完整配对：两边通过，后续重复未完成

[首次完整配对审阅](qap-first-pair-review-001.json) 补齐 control 的确认和最终审计，两份排列与目标值独立重算一致；与此前四份记录合在一起，首次两组的开发、确认、审计六份输出均已独立核对。control 在 **54.150 分钟**通过，candidate 在 **29.314 分钟**通过；这一次配对的达标时间下降 **45.8652%**，不是完整 QAP 两次均值或跨领域收益。

共同审计参考目标值 218.551647，control 为 201.188037（改善 7.9449%），candidate 为 199.849531（改善 8.5573%），两边均通过原效果及不确定性门槛。control 的实际训练加开发、确认、审计 worker 耗时为 **1071.76 / 986.79 / 1008.56 秒**。源码将 1120 秒预测预算通过短单线程标定换成带 0.9 系数的固定迭代数，再以两个线程执行多次独立随机重启；candidate 则在定长阶段间扰动已有解并检查时间。算法细节、随机数与预算机制共同变化，不能作单因素归因。

control 的六训练例内部检查测得 196.9042 对 217.938，约改善 9.65%，但内部 scipy 参考只有四次重启；它明确披露与共同十六次重启参考不同。最终宿主验收使用正确的冻结参考，不能拿这个缩减预算的内部比较替代外部证据，也不能将训练自报的 65.3 秒当完整研究时间。

两边各两次已知用量请求、各一份付费未使用提案，均已结算；整个池仍未结算或释放。当前 **30/32 闭合、22 次通过**，最后两次 QAP 已按原计划请求方法。视觉和回归两个完整领域仍更慢，原默认门槛不满足，30% 平台目标未完成。


## 完整终态（2026-09-23）

32/32 完成、24 次验收通过，完整独立核验和请求结算通过。等权平均达标时间 54.622 → 36.515 分钟，下降 33.1486%，但视觉/回归更慢、领域区间含退步，不满足默认升级条件。QAP 两次任务均值 42.699 → 27.943 分钟；新增双方第二次六份预测独立重算与原值一致。具体失败、方法限制、预算结算及下一开发对照见 [完整结果](RESULTS.zh-CN.md) 和 [QAP 完整审阅](qap-complete-review-001.json)。


## 确认失败后立即转审计：单条有利轨迹不足以推广

[闭合 32 次轨迹的只读诊断](post-confirmation-audit-diagnostic-001.json) 检查一个具体候选规则：唯一确认失败后，对随后第一个达到相同开发门槛的严格新最佳方法立即做最终审计。9 次确认失败中，7 次出现这样的后续方法；其中 4 次与实际最终审计的方法相同，但仅 1 次最终通过。另 3 次会改选未独立验收的方法，其中 2 次替代了后来成功的 TREC 方法，不能凭开发损失推断它们也会成功。

唯一相同且成功的轨迹是 KMNIST 第二次 candidate，其开发完成至实际交付间隔 1500.16 秒。这个间隔包含后续探索与最终审计，不是已证明可节省的时间；较早结束仍然失败的方法依旧计 7200 秒。固定观察序列不能复现提前停止后的资源争用、按墙钟预算运行的推理或验收质量，因此不估计全局加速比或新成功率。

决定：不根据这条有利轨迹替换确认失败后的继续探索，不据此新增默认策略。若之后仍要检验提前审计，须另行冻结开发对照并保留后续新任务验证。本诊断没有执行候选、加载检查点、请求研究模型或启动 GPU；当前确认选择开发实验保持原协议。原始依据哈希和所有九条轨迹均保留。
