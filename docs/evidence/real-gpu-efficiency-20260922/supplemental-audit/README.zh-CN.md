# 已完成 12 回合的补充核验（非最终均值结论）

截至 2026-09-22 18:27 UTC，读取 12 个已完成且不再变化的回合结果，
重新计算 66 份开发、确认、最终审计预测的评分/比较，全部与原始记录一致。
同时核对交付源文件、权重指纹、调用关闭和轨迹记录。两组各完成 6 回合、
各验收通过 4 个；剩余 4 回合继续运行。冻结的全局独立验证尚未完成。

耗时按每回合开始到真实交付事件分段，模型请求重叠不重复相加：

| 已完成六对的累计分钟 | control | improved |
|---|---:|---:|
| 仅模型请求进行中，GPU 容器未执行 | 168.34 | 53.84 |
| 仅 GPU 容器执行 | 58.98 | 55.10 |
| 模型请求与 GPU 容器重叠 | 0 | 15.15 |
| 其他控制开销 | 0.32 | 0.35 |

这些是匹配的已完成回合累计值，不是全实验实际经过时间，也不是全 16 回合
的均值。GPU 容器时间包括加载、训练、存盘和推理，不能解释为 GPU 核心利用率。
原始提供方请求时长仍单列保留，不再与 GPU 容器时间相加成端到端总时间。
累计无效尝试为 19 对 2，改进组有 3 个付费但未使用的提案。执行组合同时改变
输入兼容、输出格式和并行准备；当前实验不能单独归因各组件的因果收益。

研究能力方面，成功记录出现了数据约定诊断、训练内保留集检查、解码规则、
LoRA 与原模型插值等探索。TED 第二次改进组没有更新权重，主要收益来自
转写约定对齐，不能声称声学能力提高。AMI 第二次两组开发集改善均未通过
最终独立验收，说明执行可靠性提升尚未解决泛化失败。下一步仍需完成全局
验收与均值判断，再决定默认执行方式；本快照不改变默认。

重算命令（只读原始研究目录，输出必须为新文件）：

```bash
python3 analyze.py --study /path/to/real-gpu-efficiency-20260922 --output /path/to/new-report.json
```

`analyze.py` 可处理部分或全部回合；只有 16 回合齐全且冻结全局验证通过，
才输出整体主指标均值。区间分段算法额外通过三组重叠、裁剪、完全重复案例。
