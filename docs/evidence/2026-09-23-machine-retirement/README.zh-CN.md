# 机器收回归档核对

用户于 2026-09-23 确认机器收回。结果解释、代码状态和恢复步骤见[最新交接](../../plans/2026-09-23-machine-handoff.zh-CN.md)。

此仓索引覆盖 52 个已有研究归档目录、870 个已跟踪文件，以及 6 个产品代码入口。归档前 main 为 `93282d3ab1c35f7ff540a79dfd46099b6ed26f2e`；本次没有改写历史结果或实验协议。

从仓库根目录运行：

```sh
python3 docs/evidence/2026-09-23-machine-retirement/verify_archive.py
```

仅使用 Python 标准库和 Git；读取已跟踪文件，按路径、大小和 SHA256 计算目录摘要，不执行候选代码、反序列化检查点、调用模型或启动 GPU。完整性通过不代表科学有效性通过，也不证明 Git 外的数据和环境仍可用。将来修改产品代码会触发代码摘要差异，需与交接时版本比较。

私有 Pro 同目录另存 `source-coverage.json`：最后一批 63 个准备脚本均与此前已提交的直接文件或 `records.json` 内 UTF-8 原文逐字节相同，无缺失。Core 不重新复制私有实现。

Git 中已有的原始失败、部分回执和核验限制原样保留。大数据、特征、训练权重、私有划分种子、凭证、Enroot 根目录及依赖二进制没有作为此次 Git 归档上传；完整执行恢复需要另行取得或重建，详见交接。
