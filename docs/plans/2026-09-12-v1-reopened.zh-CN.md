# V1 完成审计：撤回提前收口并继续实施

日期：2026-09-12。状态：进行中；原 V1-00～07 尚不能宣告全部完成。

## 为什么重开

以冻结的 [原始方案](2026-09-10-autoresearch-v1.zh-CN.md)逐条检查真实代码和测试语义后，发现 2026-09-11 汇总过早。旧汇总的源码指纹、JUnit 数量及远端记录仍为真实历史；记录一致性不能证明全部产品边界。不可把原条件改写成现有实现的较小子集。

重新审计起点：Core `69ff7babb57371632fb3048db781b24a3545548a`、Pro `328cad547c12d2262971cbc20b921d17d31710f4`；源实现分别仍为 `92e388b9f7e1a1bc822695c2f1c52d4e43ab3b79` / `1384c12d40069c98a430d0e25647de102a7e38de`。两仓 main 未变，没有部署或真实 GPU/provider 调用。

## 必须处理的事项

| 项目 | 当前证据与缺口 | 关闭条件 |
|---|---|---|
| C1：领域无关的覆盖资格 | `reporting.evidence.dataset_metric_keys/count_dataset_metrics` 在实际资格路径按 `wer_*` 筛列或回退；只改列名可使相同数值从合格变为不合格。独立实际临时 metrics 文件→公共 qualifier 对照：普通名为 `0.0/local_evidence_verified`，重命名后为 `None/metric_coverage_below_min:1/2`。S1 字符化测试固化了旧行为，不能证明原边界 | 冻结公共入口红测；显式覆盖声明、重命名不变、聚合/时间列不冒充数据集、重复/缺失/非有限/声明源负例；同一声明进入缓存/历史身份；Core/Pro 真实消费者与旧配置迁移证据、全量及远端核验 |
| C2：配额不阻断实验的联合证据 | 同账户退避/健康账户角色的现有断言成立，但未找到同一真实控制循环同时证明“配额退避 + 实验继续完成”的联合断言；单独绿色模块与源码调用顺序不替代它 | 真实 Orze + Pro 同循环，在不调用 GPU/provider 的私有 CPU-backed 实验中证明同账户不重启、健康账户独立、数据面实际完成；测试替身不能替代调度或实验核心行为 |
| C3：运行中租约超时 | 原方案明确要求运行中的租约超时与 worker 丢失处理。当前已核实启动前 LEASED deadline、实际 worker wall timeout、监督丢失 HOLD；启动时 lease_until 被清空，不能把前两种不同语义拼成一个持续运行租约证据 | 检查并明确现有运行 authority/deadline 是否满足该原条件；不满足则实施最小持续租约到期处理及故障测试。未知副作用不能自动重派，不借此扩为远程集群或通用进程接管 |

按 C1→C2→C3 收口，不并行修改相互依赖的生产行为。独立的只读审查及新增隔离测试可以并行。C1 的兼容取舍必须明确：不能为旧 `wer_` 启发式保留一个实际资格后门，也不能把所有显示列都计作数据集使聚合值填足覆盖率。无覆盖门的旧配置继续运行；依赖隐式覆盖的配置须显式给出覆盖语义，不自动修改用户项目。

## 证据和状态

本轮实施进度：C1 生产修复 `7d4cabf`，显式覆盖声明进入 Core/Pro 当前消费者；Core 347 项目标回归、独立冻结 47 项复验及 Pro 108 项目标回归通过。首次 Pro 完整全量为 982 passed / 1 failed，唯一失败是 C2 联测暴露的真实 legacy shutdown 容器接口错误，不能记作全绿。

C2 修复 `e449bf8` 只改退出时的入口快照与按对象身份移除，保留 HOLD 和回调换入的条目，不扩充伪映射接口。旧实现独立 10 项边界为 8 failed / 2 passed；修复后包含该文件的 52 项 shutdown 回归、独立 10 项复验及原冻结真实 Pro 联测通过。双仓完整全量现已完成：Core 4365 passed / 7 原有可选依赖 skipped / 2 原有 warnings；Pro 983 passed，原配对 60 passed。995 个源码/测试/示例/构建文件前后指纹一致，完整 JUnit 和失败历史均保留，见 `docs/evidence/2026-09-12-c1c2-core-full.json` 与 Pro `7e822298` 的对应证据。

C1/C2 在上述有限合同内关闭，解锁 C3。[C3 简化合同](2026-09-12-c3-runtime-lease.zh-CN.md)已冻结；原行为真实 2 failed / 1 passed 诊断保留，不以导入缺失冒充红测。运行期租约尚未实施验收，整版仍未完成。

补跑当前源码真实 CPU 策略、领域、自产分析、终态崩溃恢复四模块：24 passed / 1 warning；Pro 配额、provider 分类、方法上下文和提示预算四模块：39 passed。这些是新执行的限定回归，不冒充重新全量，不关闭上表缺口。此前完整全量仍属于相同源码的历史兼容结果。

旧 2026-09-11 canonical、测试失败历史和固定提交保持可追溯；本页及当前账本明确撤回它们被用于“整个原 V1 已完成”的推论。每项补充都要保留基线/修改/真实结果/独立复核，并更新远端。研究收益、真实 GPU/provider、生产授权和部署仍未验证。
