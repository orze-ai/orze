# 预算与 ingress 性能片：实施补充

用户已批准执行[后续性能合同](2026-09-14-history-cost-next.zh-CN.md)。起点 Core `e606ba6688b4e1972e26a08617821a427668dd24`、私有 Pro `879859cb383d7f113f58ba1da054c6785e4fce6a`，在同一验证分支继续；不改用户 ASR 项目，不切换生产服务。

## 一处已确认的测试夹具迁移

旧 `tests/test_idea_ingress_contract.py::test_stale_id_snapshot_cannot_replace_a_concurrent_winner` 把真实第二连接的竞争提交挂在 `get_all_ids()` 返回之后。消除这个全表查询也会移除其故障注入点。

原计划“原有测试不删改”在此明确补充一个例外：只迁移该函数的注入方法及参数到新定向 ID 查询之后，仍在正常 insert 之前。保留原测试全文、原基线通过、去掉旧查询后的注入失效记录，以及最终同业务断言的通过。不得删除／改弱任何业务断言，不以检测 mock、空调用旧查询或测试专用产品分支制造兼容。

最终旧测试审计必须分开报告：其余原文件逐字节不变；此唯一文件仅该夹具位置变化，原断言及真实竞争写入仍生效。不能再笼统声称本轮全部旧测试文件字节不变。

## 差分校验不是保留旧漏洞

新单次预算查询必须同时读取并核对 SQL `reservation_id` 与 permit 内的 ID。旧代码在扫描中按 permit ID 再次主键读取，可能把另一条已存在记录当成本行复验对象；独立测试将验证这一推断。

如果真实差分发现旧版接受这种错配、新版拒绝，应单列为有证据的完整性加固，不放松新校验以追求错误行为等价。其他合法预算结果、精确累计、状态检查及原拒绝边界继续保持。预约表和索引结构不变，不引入摘要账本或研究总时限。

## 交付

先完成基线、定向差分与规模测量，再统一冻结源码／测试并跑两仓回归、实际 CPU／恢复路径及隔离制品验证。原始失败分清产品缺陷、性能基线与夹具迁移；Pro 原始私有清单仍只进私有仓。验收后提交推送，并读回远端确认。
