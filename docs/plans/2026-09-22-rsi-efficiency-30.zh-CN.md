# 再降低 30% 研究达标时间：论文、瓶颈和首个候选

状态：三个候选开发对照已完成并核验，均不支持默认升级。旧 TSP 的开发目标不可达已通过数学证书独立核验。30% 目标未完成；六个新数据集共同参考已核验，独立优化校准已启动，尚未冻结或启动效能试验。原跨领域试验已全部结束并核验；原文件保持冻结。

## 成功标准

相对当前已部署默认（Core `fda61cc` / Pro `d5ca768`），在新的前瞻对照中，将**跨领域平均最终验收达标墙钟时间至少降低 30%**，同时成功率不下降。失败、模型等待、诊断、执行和独立验收全部计入；按问题/领域等权、报告配对不确定性和领域退步。不能用 token、费用、程序有效率、单任务最快轨迹或事后挑出的成功者均值替代该目标。

正在运行的 `cross-domain-research-20260922` 只评估此前冻结的 control/inquiry。它不能验证此后写出的 pilot 能力。其操作日志用于定位下一轮的开发瓶颈；新机制的验证任务必须在机制冻结后评估。当前已看过的结果不能重新包装成该机制的独立验证。

## 阅读原文后的取舍

以下采用作者论文/仓库的一手材料；日期为预印本版本日期，论文自己的数字不是 orze 的收益。

| 文献 | 可取机制 | 对本目标的限制和采用方式 |
|---|---|---|
| [RRSI，09-21](https://arxiv.org/html/2609.24972v1) | 少量可归因改动、保存被否定的机制、按迁移表现和复杂度选择 | 论文约 30% 的节省是相对未正则化演化的 policy tokens，不能等同达标时间。采用“一次优先检验一个机制”，无收益即删；不移植整套 critic/pruner。 |
| [SoL-Pi，09-17](https://arxiv.org/html/2609.20519v1) | 合并可连续执行的动作；大观察保留原文且可按需读取；验收前冻结 | Efficiency 配置的 token 降幅不代表能力完全无损：Opus 5 表中平均分保留 94.3%。采用其减少无用中间回合的思路，能力门槛仍按本项目成功率定义。 |
| [Dream-RSI，09-14](https://arxiv.org/html/2609.14858v1) | 从已有发现轨迹构造回放环境，学习探索分配 | 回放只能揭示记录过的节点；改了上下文/程序后不能把旧结果当新结果。继续复用已有回放接口，最终必须在线验证。 |
| [RSIAgent，09-14](https://arxiv.org/html/2609.15364v1) | 针对知识缺口安排探索；成功、失败与适用条件一并保留 | 原文指出练习未命中弱点、验证不完整、错误记忆固化等失败。作者仓库也披露非匹配重试/检查点口径；不照搬其平均收益。采用“针对解释做实验”，不强制探索配额。 |
| [ASPIRE，08-31](https://arxiv.org/html/2608.31111v1) | 隐藏真实下游考题，检验自我提出的训练/验证目标能否迁移 | final-only 的 24 次权重演化仅 3 次超过起点，不能把完成训练循环称作能力增长。pilot 自报成功也必须经过同一个外部验收。 |
| [Meta-Harness，03-30，较早基础工作](https://arxiv.org/html/2603.28052v1) | 按需检索完整历史源码与轨迹，保留诊断所需信息 | 完整历史可访问不等于每次全塞进提示。当前研究调用没有交互检索工具，不能只换成不可解引用的句柄就声称等价。暂不做这种上下文删减。 |
| [The Last AI Built by Humans，09-10](https://arxiv.org/html/2609.11873v1) | 区分任务内改善与持久系统改变 | 该综述把仅改善当前输出的 B0 与持久系统改进分开。orze 的新机制必须在未来独立任务有效，不能靠当前任务调参结果宣称 RSI。 |

以上启发了下面的工程假设，**论文没有直接证明此 pilot 接口能提高科学研究效率**。

## 实际瓶颈审计

`rsi-efficiency-20260922/bottleneck-audit.json` 固定了一次只读快照及每个输入的 SHA256，包含当时已结束的 27 次研究；它是部分轨迹诊断，不是两组最终胜负比较。

- 总 episode 墙钟 26322.57 秒中，仅实验容器执行占 16870.31 秒，约 **64.1%**；仅模型等待占 7790.76 秒，约 **29.6%**；二者重叠 1606.53 秒，其余 54.96 秒。
- 此处为各 episode 时长之和，不是全机器经过时间；容器时长包含 CPU/GPU 运算、加载和保存，不等于 GPU 利用率。等待和执行重叠没有重复计数。
- 对当时 118 份提示做完整 source 字符串检查，**单份提示内重复 source 字节为 0**。没有据此实现“源码去重能提速”的空优化。
- 共享全历史使后续提示和保守预留增长，但删除过去反例也可能伤害研究选择。最终需要能力对照，不用提示长度下降代替验证。

即使把“仅模型等待”全部消除，该快照的直接墙钟空间也只有约 29.6%，且不能解决最终未达标的任务。首要候选因此放在实验执行和信息价值上。

## 首个候选：可继续的小规模预检

新增 Core `orze.research.pilot.run_training`，显式 `allow_pilot=True` 才启用。默认仍执行原来的 build/train；未修改冻结试验读取的任何源码。

研究模型可在原 build/train/predict 程序中增加 `pilot(api)`，自行选择训练数据内部的小规模测试、相互区分的预期以及是否值得继续。不固定算法、不规定先调哪些参数、不强迫每次都做 pilot。

```python
def pilot(api):
    # 执行程序作者选择的小规模测量；这里不写研究答案。
    return {
        "continue": measured_decision,
        "findings": measured_findings,
        "model": optional_partially_trained_model,
    }
```

- `continue=False`：跳过 build/train/预测/模型保存，返回无分数诊断，保留实际 findings；仍消耗一次提案及真实时间。
- `continue=True`：可以接着训练原来的部分模型，避免重复训练；没有部分模型则正常 build。pilot 的测量快照不会被后续训练改写。
- 数据权限、总资源/超时、模型请求数和最终评估器不变。小规模检验默认只用允许的训练输入；没有新增确认/最终标签访问权。
- pilot 抛异常是实现失败，不是对科学假设的反证；没有自动重试。内部 findings 不具有外部得分/验收权。
- 隔离 worker 根据实际结果 kind 将终止的 pilot 标成 analysis。本开发对照的控制器按实际 kind 跳过开发评分/模型资格检查，不能仍因源码具备 train 函数而把它当完成的方法。
- 最终预测只重载已保存模型，不重做 pilot。完整训练与 pilot 都在同一个原有时限内，没有额外免费计算。

预检程序是人工编写的接口测试，验证了停止、继续复用、旧方法兼容以及无标签独立重载。最终版本的四个隔离 CUDA 容器均正常闭合；32 项 Core 测试及 11 项控制器集成测试通过。它们不能作为 30% 收益的实验数据。

## 后续验证与费用边界

1. 现有 32 次前瞻对照继续运行，结束后按它原来的规则核验和处理默认决策。开发对照只使用它已结束的 Fashion / 电厂世界，以及空闲的 GPU 0–3，不修改其冻结输入。
2. 在开发任务验证 pilot 是否真的减少无信息的长实验；失败和漏掉的好方向都保留。随后冻结候选，再做新的跨领域在线配对，不能用旧轨迹回放估算出的时间宣称真实收益。
3. 如果共享证据和 pilot 一起测试，必须明确是组合效果；不能把收益单独归因给任一部分。较小的预检也可能误杀好方向，最终成功率必须检查。
4. 继续遵守累计 $5000 授权；当前 $160 池保持原账本。新一轮发请求前必须释放已有确实结束且可核验的未用预留，或从已确认余额分配，不能以计划金额当作新增授权。
5. 已复核 [Anthropic 官方定价](https://platform.claude.com/docs/en/about-claude/pricing)：标准 Opus 5 的输入、缓存和输出单价不同。后续可在确认请求模式及完整用量后改进过于宽松的成本上界，但不修改当前试验预算规则，也不把这项账务变化冒充研究能力提升。

目标只有在完整前瞻结果、最终验收、来源/费用/执行闭合核验共同支持时才算完成；目前仍缺这部分证据。

## 已完成的开发对照（负结果）

四次研究比较当前默认与“预检提示＋可选预检权限”，两边都是 ParallelRefine，未同时加入 inquiry 共享证据。每次最多四提案、两路径各两步，独立验收规则相同。预算池 $18，直接取自现有未分配额度；累计保守分配 $4998.938525，旧试验 $160 池未释放。任务此前已被观察，开发通过不能用于默认升级或 30% 泛化验收。详见私有运行目录 `rsi-efficiency-20260922/PROTOCOL.zh-CN.md`。

## 新发现：当前能力评测存在顺序线索

源码审阅及六次独立预测证实，sentiment-1 的一条 inquiry 高分依赖类别排序；只打乱输入行顺序，其错误率由 0.67% 变为 11%，不再通过相同目标。旧原始结果保留，不能用这条收益作为通用能力/30% 目标证据。后续切分修复已在原始语料验证，详见 [有效性诊断](2026-09-22-cross-domain-validity.zh-CN.md)。候选的最终验证仍需新的前瞻任务，重排旧语料仅为开发预检。

## 开发结果与下一步取舍

四次研究已全部结束，21 份预测独立重算、15 个实验容器闭合、11 次模型请求用量全部已知，验证进程退出 0。

| 开发任务 | control 最终验收 / 达标时间 | pilot 组最终验收 / 达标时间 |
|---|---|---|
| Fashion-MNIST | 通过 / 5.47 分钟 | 通过 / 13.64 分钟 |
| 电厂回归 | 通过 / 14.42 分钟 | 失败 / 按协议记 120 分钟 |
| 两任务均值 | 2/2 成功，9.94 分钟 | 1/2 成功，66.82 分钟 |

这是两个已观察的开发任务，不能作通用均值结论或精确因果估计。它足以拒绝把当前候选升级为默认。5 次 pilot 组付费提案均未定义 `pilot()`，所以更不能把差异归因于预检执行本身。Fashion 的两条候选采用了相近的增广与集成；control 实测训练 202.2 秒，pilot 组 657.8 秒，后者每个成员训练约 347–401 epochs，花满时间上限的行为没有带来更快达标。

本轮另有 1 次无效提案、1 次预算未准入、2 个已付费未使用提案，全部保留。不得因为机制未被采用就强加固定预检配额来制造 adoption；不再扩展该接口。下一候选优先使用**已有的实测训练/预测时长与原模型**指导实验规模、继续条件和复用决策，先检验更小的决策改动，避免再叠加新执行协议。这是由负结果提出的下一假设，不是已证实收益。

$18 开发池结算后保守保留 $6.468675，已确认释放未使用预留 $11.531325；累计保守分配变为 $4987.4072，剩余授权 $12.5928。正在运行的跨领域 $160 池及 25 条历史未知记录未释放。账本在 `budget-release.json`；估算实际 API 费用 $4.606315，不以估算金额替代保守上界。

下一轮前瞻输入须用此前未用于本平台开发的数据。已检查现有计划并排除曾用的 wine-quality / concrete；当前考察 CIFAR-100、KMNIST、AG News、TREC、Abalone、SARCOS（第一关节力矩），另配新生成的优化实例。数据源与任务方案尚未冻结，不能声称已开始新泛化验证；重排旧语料只用于切分修复预检。

来源：[CIFAR](https://www.cs.toronto.edu/~kriz/cifar.html)、[KMNIST](https://codh.rois.ac.jp/kmnist/index.html.en)、[AG News 数据处理论文](https://arxiv.org/abs/1509.01626)、[TREC](https://cogcomp.seas.upenn.edu/Data/QA/QC/)、[Abalone](https://archive.ics.uci.edu/dataset/1/abalone)、[SARCOS](https://gaussianprocess.org/gpml/data/)。这些是已有公开基准，不声称基础模型从未见过。


## 第二候选：实测耗时反馈与计算分配

首个 pilot 开发候选被否定后，冻结了更小的组合干预：把已闭合参考及本分支实验的实测容器/worker 墙钟传入研究上下文，增加按问题所需信息决定实验规模、停止条件和兼容模型复用的指导。原参考 Fashion 容器耗时 78.61 秒、电厂 6.75 秒，此前 root 上下文只有源码和开发分数，未显式提供这些计时。

不增加接口、工具调用、共享分支证据或固定训练配额；也不要求选择最便宜的算法。默认仍关闭，两个已观察任务各 control/effort 一次，最多 16 请求。11 项控制器检查通过，特别检查未知计时不填零、不读取确认/最终评估数据、验收失败不能写成开发成功，以及交付不必等并发请求结算。

开发池 $12.50、每 episode $3.125，来自上一轮已核验释放的余额；累计保守分配 $4999.9072。运行输入和方案已冻结，计划位于 `rsi-effort-20260922/plan.json`。本轮结果只决定是否进入新任务验证，不能直接宣布 30% 或升级默认。两组预算一致，但池小于上轮 pilot，不能把两轮均值差异归因给机制。


## 第二候选结果：约 1.1% 的开发均值差，未支持 30% 目标

本轮四次研究已完成并独立核验：21 份预测重算、15 个容器闭合、9 次模型请求用量全部已知。两组均 2/2 最终验收通过。

| 开发任务 | control 达标时间 | effort 达标时间 |
|---|---:|---:|
| Fashion-MNIST | 6.76 分钟 | 10.29 分钟 |
| 电厂回归 | 12.61 分钟 | 8.86 分钟 |
| 均值 | 9.68 分钟 | 9.58 分钟 |

描述性均值只减少 1.10%，两领域方向相反，配对均值差的 95% t 区间为约 -46.38 至 +46.17 分钟。两个已观察开发任务不能证明这种小差异可复现，更不证明 30% 或通用收益；保持默认不变。不能因控制组这次比上次慢就把重复运行间的噪声算作机制提升。

实测耗时本身没有可靠地改善计算分配：Fashion 的 effort 提案把参考仅用约 74 秒描述成“浪费了约 90% 的可用计算”，仍设计较大的从头训练集成。9 份付费返回源码都没有引用 parent_dir，未获得模型复用带来收益的证据。下一步不能继续堆叠泛泛的“更省时间”提示，也不强迫无依据的提前停止；应针对实际未解决的科学问题和可区分的候选解释，检验能否减少达到同等证据所需的完整实验次数。

本轮 0 次无效尝试、1 次预算未准入、2 个已付费未使用提案。估算 API 费用 $3.58539；保守结算 $4.90725，释放 $7.59275。累计保守分配 $4992.31445，剩余授权 $7.68555，运行中的跨领域 $160 池与历史未知费用保留。

## 新验证输入与再次发现的数据偏差

六个此前未在平台开发计划中找到的公开数据集以及两类新生成优化实例，已准备两次划分共 16 个世界。12 个源文件下载校验、192 个生成输入文件与实际跨集合特征重叠检查均通过；没有运行研究候选，没有观察候选得分，没有冻结效能试验。

新发现：下载的 SARCOS 源测试 4449 行中，4428 行的双精度输入与源训练完全一致，4425 行所有目标也一致。已经对相同任务观测去重，按输入组重新随机划分；不把该源测试直接当独立验收。只用前 21 列预测第 22 列的第一关节力矩，其他力矩列未进入特征。来源及限制详见 `docs/evidence/runs/2026-09-22-rsi-fresh-inputs/INPUTS.zh-CN.md`。

精确去重不能证明相关新闻事件/机器人轨迹的完全独立，也不支持时间外推。新 TSP/QAP 的目标可达性尚待预检，这些输入不能被当作已验证合理的最终效能任务。后续评测必须在新候选运行前冻结任务、目标和决策规则，不根据候选结果挑任务或改阈值。


## 第三候选：用实际错误指导假设，而非只展示开头样本

重新检查既有轨迹发现，Fashion 两个参考在全开发集分别错 136/127 例，但原提示的前 12 例全部正确；旧文本任务前 12 例只覆盖一个类别。这个反馈既可能隐藏反例，也可能给模型错误的类别分布印象。原记录与逐文件 SHA256 已保存于 `rsi-evidence-20260922/blindspot-audit.json`。

新增可选的分类错误反馈：总体错误率、每类支持数/错误数、主要混淆，以及最多四个随机反例和四个正确对照。文本附真实特征片段；明确抽样富集了错误，不能冒充总体发生率；相关模式只用于形成待测假设，不是因果证据。仍保留完整原始记录，不添加 LLM 调用、强制算法或探索配额，不新增执行接口。这个候选改变模型获得的测量信息，而不是再加一段泛泛的效率指导。

8 项 Core 检查、10 项控制器检查通过，覆盖原前缀遗漏错误、缺失类别不能填伪率、正确/错误例子来源、最终评估文件不进入反馈等问题。开发试验只用已观察的 Newsgroups 语料，按修复后的私有随机分组重新训练参考；旧 checkpoint 不得跨新切分复用。两组仍使用完全相同提示、模型、ParallelRefine 和验收；只改变开发证据。profile 的计算时间也计入 episode 墙钟。

计划已冻结：一对研究、GPU 0/1、最多八请求、总池 $7.50、每组 $3.75；累计保守分配 $4999.81445。旧跨领域 $160 池未释放。单个已观察分类任务不能证明跨领域 30% 或估计跨任务置信区间；本候选也不自动给回归/优化新增能力。默认保持不变，完整目标继续保留。

这次取用 [RRSI](https://arxiv.org/html/2609.24972v1) 的依据测量归因与改变未充分探索组件、[RSIAgent](https://arxiv.org/html/2609.15364v1) 的针对知识缺口探索思路。二者并未证明本反馈有效，开发及未来独立验收仍是判断依据。


## 数学诊断：旧优化任务的提前达标门槛存在不可达问题

192 个 TSP 实例的下界证书已独立核验。TSP-0 / TSP-1 的开发集最多可能相对参考改善 1.63525% / 1.99386%，都低于原 2% 目标，故提前确认路径不可达。两个最终审计集的上限为 2.17387% / 2.28159%，仍不足以判断可达性；原来预算结束后审计开发最佳方法的路径没有被排除，不能说整个研究必然失败或事后剔除旧失败。

校验器不调用原搜索，使用树的环条件、606,720 次精确舍入检查以及原宿主评分；两项运行试验共 125 个冻结文件保持不变。没有付费研究请求。详细推导和范围见 [TSP 目标可达性](../evidence/runs/2026-09-22-rsi-feasibility/RESULTS.zh-CN.md)。这是一项评测有效性发现，不是 30% 收益。

下一轮优化任务必须在研究候选运行前检查目标空间，并以有效解证明目标具有可达见证；下界未排除目标不能当作可达证明。不能为制造收益降低参考、按候选输赢调阈值或删除优化领域。新输入尚未冻结为效能试验，仍需独立校准和两组共同协议。


## 第三候选结果：提供错误证据未达到同等研究目标

开发对照已完成且独立核验有效：control 在 6.71 分钟最终验收通过；evidence 实际 15.46 分钟交付，但最终改善 8.5366%，低于固定 10%，按协议记 120 分钟失败。两组程序均有效。12 份预测独立重算、12 个容器闭合、5 次请求费用已知。单个已观察任务不能推出一般性负效应，但足以拒绝将当前候选默认启用。详见 [错误证据开发结果](../evidence/runs/2026-09-22-rsi-evidence/RESULTS.zh-CN.md)。

记录显示新增证据被使用，但更复杂的解释没有带来更快验收；最后的文本图传播系数实际为 0，不能按提案文字把它记作有效方法。不要把“提出了更多假设”算作能力提升。

新诊断发现四个已执行方案的内部验证均按行切分。重建源码声明的切分后，约 64.67%–67.34% 的内部验证行与内部训练共享主题组，外部评价却要求组隔离。它影响两组，尚未证明改变选型或耗时的因果效应。原 `/train/groups.json` 已挂载但未被 API/提示明确呈现；下一候选应让研究程序知道实际验证单位和已有组信息，检验内部实验是否更有预测力，不添加强制反思或固定算法。

本轮保守保留 $3.568925、释放 $3.931075；累计保守分配 $4995.883375，余额 $4.116625。原跨领域 $160 池及历史未知记录继续全额保留。


## 正在运行的内部验证诊断

在新增模型请求前，先对上述已结束的两份方法各做原始/按组切分的配对重跑，共四个固定源码任务。只改内部验证单位，用训练组和已观察开发集核查是否改变选型；不使用确认或最终审计数据，不修改原研究，不作为自主研究或 30% 证据。计划、源码已冻结，详见 [诊断边界](../evidence/runs/2026-09-23-rsi-validation/DIAGNOSTIC.zh-CN.md)。


## 已完成跨领域结果：描述性下降 34.13%，不满足目标证据要求

32 次研究全部结束并结算，原协议成功 control 9/16、inquiry 12/16，四领域等权平均计分达标时间 58.934 / 38.820 分钟。虽然描述性下降 34.1304%，领域配对 95% t 区间为 -84.70 至 +44.47 分钟，只有 2/4 领域更快，未满足预注册默认门槛。加上已复现的文本排序捷径和 TSP 开发目标不可达问题，不能将该数字作为 30% 目标已完成。详见 [完整结果](../evidence/runs/2026-09-22-cross-domain-research/RESULTS.zh-CN.md)。

235 份预测重算、237 个容器闭合、115 次模型请求用量已知，机械核验通过，核验进程退出 0；科学有效性限制另列。保留默认，保留全部 11 次失败和原统计；不事后剔除可疑/不可达实例计算有利子集。

原 $160 池保守结算 $75.185075，释放 $84.814925。当前累计保守分配 $4911.06845 / $5000，余额 $88.93155；25 条历史未知记录保持覆盖。新任务尚未发送研究请求。

## 内部验证诊断完成：更贴近目标的测量未产生一致选型收益

四项固定源码实验全部有效并核验，原始两个方法完整开发预测重现。按组切分使内部准确率更接近外部开发集，但 control 开发错误从 25.5459% 降到 25.2183%，evidence 从 26.4192% 升到 26.7103%，执行也未更快。因此不将它宣布为效率候选收益，不新增强制流程；只把既有组信息的明确呈现纳入今后任务协议。详见 [诊断结果](../evidence/runs/2026-09-23-rsi-validation/RESULTS.zh-CN.md)。

下一步保留已有 inquiry 作为待验证机制，先对新任务做共同参考预检和独立的目标可达性校准，再冻结新的完整四领域效能比较。三个小型负/不足候选不叠加进新方案，完整 30% 目标继续开放。


## 新共同参考已核验，优化任务仍需校准

六个新真实数据集的 12 组共同参考完成：24 次训练、36 次独立重载预测、60 个容器结束。10 组完全重现；SARCOS 的最大预测差异为 2.84e-14，原精确检查失败保留，另行冻结的数值容差恢复通过，未重训或重选参考。CIFAR-100 输出类别、KMNIST 翻转规则和已有训练组信息已正确呈现。详见 [参考预检](../evidence/runs/2026-09-23-rsi-fresh-preflight/RESULTS.zh-CN.md)。基线成绩、数据有效性修复和研究候选最终成功分开统计。

已启动独立优化校准：四个预先定义的原/替代分布各两批 64 个实例，保留原参考强度，用旧固定求解器提供可达见证；八张 GPU 共 24 次执行，0 次模型请求。所有规格结果均保留，选择规则在首个执行前冻结；不按未来候选输赢改目标。详见 [校准协议](../evidence/runs/2026-09-23-rsi-optimization-calibration/PROTOCOL.zh-CN.md)。完整四领域研究对照尚未开始。

### 新读两篇 RSI 原文后的具体取舍

- [SIFT，2609.19526v1](https://arxiv.org/html/2609.19526v1)：分离探索与评估，用源码成对评审排列探索优先级。其消融将主要加速归于异步执行；评审是排序信号，仍需实际最终测试，且最佳配置使用更强评审模型。对 orze 的推论是先量化串行等待，再判断额外评审的净收益；已有并行准备不能重复计为新提升。当前不新增评审调用，也不拿“有潜力”替代实测分数。
- [MedRSI，2609.24838v1](https://arxiv.org/html/2609.24838v1)：从重复失败定位缺失能力，组合工具或训练新模型；实验候选与稳定工具分离，冻结后在后续群体确认再注册。论文也报告高开发成本，临床指标不等同研究达标速度。这里保留快速探索与独立后续验证，不移植临床优先级或强制多层流程，不把更多工具当收益。

原有七篇论文与这两篇一起支持小范围可归因改动、真实测量和后续验证，但不能代替本平台的 30% 证据。下一效能候选仍是已实现的 inquiry；三个负/不足开发候选不叠加。

### 完整下一轮预算已具备，尚未发送请求

核对早期 parent 试验及后续完整账本，原 $280 池尚未释放：27 次已知用量按原 $50/M 全 token 上限保留，1 次未知保留完整 $10，合计 $41.5756。释放多余 $238.4244 后，全局保守预留 $4672.64405，剩余 $327.35595；历史 25 条未知均保持覆盖。详见 [核账记录](../evidence/runs/2026-09-23-rsi-budget-reconciliation/RESULTS.zh-CN.md)。

下一轮可预留 32 次各 $10 共 $320，两组仍相同模型、六提案与执行预算，减少原每次 $5 下的未准入限制。金额变化须在双方请求前写入共同协议，不能直接比较两轮的绝对达标时间。当前仍未分配该新池，30% 目标保持未完成。
