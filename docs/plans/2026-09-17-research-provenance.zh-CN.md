# 先核对处理来源，再评价“少做实验”

## 结论

本轮得到新的实际研究证据：对三个候选的全部归档预测统一应用一个已有去重复函数后，两组原指标差距分别只剩约 **16.4%／25.2%**。
这是处理口径敏感性，不能直接解释为训练贡献，也没有覆盖或删除原评分。228 个源文件只读，前后哈希一致；没有新推理、训练、GPU 或服务变更。

同时试验了将“恰好提出 N 个新实验”改为“最多 N 个有用动作，允许空提案”的提示词。
**四对都更慢、更贵，没有减少调用或提案，已恢复原默认代码。** 保留所有失败和负面结果，不继续在这道题上调文案。

## 来源审计带来的新知识

- 24 份历史分片日志记录了共同基础模型路径、各自 LoRA 路径及合并系数；它们没有记录本次核查的解码参数、代码哈希或 checkpoint 哈希。
  当前权重文件存在不能证明历史结果绑定这些字节；本次检查未建立历史绑定，不能推断所有其他记录也不存在。
- 当前评价代码记录过非标准后处理默认值切换；当前脚本不是历史运行代码的可靠替代。命名为 `delooped` 的汇总也不证明原预测已经执行同一处理。
- 找到已有索引恢复工具，但它区分不同生产程序，本次所需本地 Arrow 不在当前目录。没有把位置配对升级为音频身份核验，也没有误用另一生产路径的索引规则。
- 固定敏感性分析在 **22.69 秒**内完成：对所有行、三个候选统一应用当前函数的固定默认参数，参考词分母和八语料等权方式保持不变。
  不搜索参数，不只处理最差样本。三个候选分别有 60／24／25 行文本改变，其中改善 15／7／8 行、恶化 42／16／17 行。
  因此这也不是默认启用去重复的依据；少数巨大错误的降低与正常重复被误删同时存在。
- 另用独立动态规划核对 201 条处理后的词编辑距离，以有理数重新核对宏平均。具体候选和完整来源清单在 Pro 私有记录。

## 真实模型对照及局限

同一个实际归档算术问题，设置“尚未计算”和“已有一次未独立验证的 CPU 结果”两个状态；每状态、每版本重复两次，共八个项目。
模型从封存数字源写分析，代码逐份检查后由真实 CPU worker 原样执行，结果与独立有理数参考比较。
两组均为 Opus 5 high、16,384 输出上限、一次请求、100,000 token 预留，正常 Pro 许可，没有重试或 fallback。

| 第二阶段四项目 | A 原提示 | B 可选提案 |
| --- | ---: | ---: |
| 生成并完成分析 | 4/4 | 4/4 |
| 空提案 | 0 | 0 |
| 平均研究进程秒 | 25.70 | 45.74 |
| 平均响应字节 | 4,397.75 | 4,863.75 |
| 返回用量价格估算 | $0.378950 | $0.580790 |
| 含人工审查、批次等待的约平均经过秒 | 147.49 | 151.84 |

完整经过时间由最终捕获与进程记录的文件时间、单进程单调时钟时长重建，属于近似观测，不是部署速度估计。
首次摘要误用重写配置的时间；保留旧摘要，最终摘要修正。四对单进程时间和已知费用均退化，没有稳定提效证据。

首批旧版组的四个提案在调用前被提示词完整性检查拒绝：实验包装在打包完成后替换文字，破坏了 manifest。
原件全部保留，新阶段改为加载准确冻结的旧模块，让提示词正常构建和校验；没有绕过检查、修改模型输出或悄悄补齐同一批。
首批新版四次调用也保留、执行与计费，因此全轮实际是 **12 次调用**，不是只报告后续成功的八次。

12 份程序均完成。独立核对每份 12 个主数量，共 144 个数值：最大宏平均／差值偏差约 0.0000431 个百分点，比例偏差约 0.0000427。
部分程序先对宏平均四舍五入再计算比例，属于可避免的精度损失；没有事后选择一个“通过”容差。
六份已有结果完整进入实际下一提示的 JSON 观察；42 份结果／输入产物字节核对通过。科学有效性标签仍为 unknown。

**评价设计也需要修正。** 原计划把一个状态命名为 answered，但其中结果未独立验证，没有展示全部结构核验。
两组选择检查语料集合、分母与计数类型，有合理依据；不能把全部后续复核判错，也不能据此证明模型不会停止。
本次执行没有发现新算术缺陷或改变排名。附加的微平均不替代规定的等语料宏平均。
这是一道相关任务的流程诊断，不是独立科学任务、多领域能力或长期效果证据。

## 验证、费用与下一步

提示词变体相关检查 84 通过、3 失败；三个失败都在角色存储预检处报 `storage_atomic_rename_unsupported`，没有抵达模型边界。
准确旧模块上的独立复跑同样为这三项失败、其余一项通过，证明此失败不由提示词改动引入。没有修改存储实现或跳过生产校验。
最终 research.py 已恢复到原始 SHA-256 `18c42aef802de1fbccb0dce9088560a6a9ba92d95b6268a00cceb320caba6f52`，无运行时代码改动遗留。

本轮返回用量估算 **$1.434580**，12 次准备／12 次返回，没有新增费用未知。
累计 **319 次准备／300 次返回／19 次费用未知**，已知返回用量估算 **$14.831107**，保守预留 **$651 / $5000**。
预留不是花费，估算不是账单，未知费用不记为零。原始源码、计划、全部提示／响应、失败、计算与核验见[证据索引](../evidence/runs/2026-09-17-research-provenance/private-evidence.json)。

下一优先级转向不同研究问题中的证据复用与实际决策质量：应有一个已独立核验的结果、一个仍需计算或反例的真实缺口，
同时衡量少做无效工作与不漏掉必要工作。停止把“已有未知有效性的输出”当作必须停止的标准答案。
原定九项长程、记忆、跨领域及受控上线验收继续开放。
