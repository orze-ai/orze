# 原交接 P3 测量要求的增量验收

依据仍是用户指定的 [558f8c6 交接清单](https://github.com/orze-ai/orze/blob/558f8c69db03eb7a5087d8200f54d4ce0fe46dcd/docs/plans/2026-09-14-machine-handoff.zh-CN.md)。
本次核对已完成的原始运行与当前实现。研究质量／整体效率的结论见[自适应研究报告](2026-09-17-research-adaptive.zh-CN.md)；以下验收针对测量方法和执行链。

| 原定要求 | 当前证据 | 判定 |
| --- | --- | --- |
| P3-1：跨领域、等质量对照；有效证据／确认选择延迟、调用、资源成本、失败与未知 | 先前实际 ML 与先前自适应 18 项目有逐组质量、延迟、费用及失败；自适应组只有一组额外收益。数据准备、人工排错和完整资源成本尚未统一计量，不能把不同质量的时间比较称为等质量提效。 | **继续开放** |
| P3-2：保留负对照、无收益任务及覆盖代价，不用名称或预知答案制造收益 | 先前自适应六组包括两组目标置乱负对照与五组无额外收益；两对负对照完整列出 R／P／F 时间和费用。任务、分组、候选列表在训练前冻结，各方案共享数据说明；选择不使用留出结果。所有原中断／未启动记录保留。逐份提示记录实际合格 ID 和跨页遗漏，容量修复明确可能挤掉普通记录。 | **测量要求已完成** |
| P3-3：实际研究链；查询数、夹具或测试通过不能代替科研收益 | 实际 research CLI、正常签名 Pro 许可、43 次已返回供应商请求、真实 CPU 拟合和留出确认；485 个不同进程正常闭合，551 份有效预测逐行复算。失败的编排、原始模型响应、用量和重放全部归档；报告把模型选择收益、来源读取速度和测试结果分开。 | **执行与报告要求已完成** |
| P3-4：有限验收停止规则与自主研究决定分开，不重引入产品总期限 | 冻结计划将一／两轮明确限定为比较预算；轮数位于独立实验脚本，没有写成产品总期限。43 份实际分页提示均为 `scientific_convergence: false`；当前 Pro 分页仍固定输出该标记。模型允许空提案；动作超时、请求额度与 CPU 预留按资源边界处理。 | **当前验收运行的边界要求已完成** |

可复核原件：

- [逐组结果与实际页面](../evidence/runs/2026-09-17-research-adaptive/summary.json)：`pairs`、`runs[].actual_pages`、完整费用及失败继续口径。
- [私有原件索引](../evidence/runs/2026-09-17-research-adaptive/private-evidence.json)：5,866 个文件逐字节验证，归档 SHA256 为 `d3a23bee1842f6b07f0342f5211825a00c5b10db8dcb82793fd6acb91f70fb80`。
- 原件中的 `plan.json`、冻结脚本、`selected-before-holdout.json`、`response-*.json`、`usage-*.jsonl`、`capture.json` 与原 `operator-stop.json` 共同支持上述结论。继续目录对已有模型响应的副本没有重复计费。
- 当前产品实现：Pro `3d77bbe` 的 `report_paging.py` 保留非收敛标记和来源校验；上一轮交付 main `d2bb936` 只另加文档与证据。

因此，当前交接中 P3 的后三项可标记完成，剩余未勾选项从九项变为六项。
仍保留 P1 的真实长程信息利用、P2 的多轮反例／重复失败验证、P2 的三项真实上线验收，以及 P3 的完整等质量成本验收。
**这没有证明稳定的整体研究提速，也没有完成“全面提升研究能力”的目标。** 新的研究对照仍须遵守已经验收的三项测量要求。
