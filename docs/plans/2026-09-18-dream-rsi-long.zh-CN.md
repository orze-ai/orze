# Dream-RSI 默认探索：前瞻长测

状态：运行中，尚无最终均值结论。

## 默认行为

`run_online` 省略 policy 时使用 Portfolio；Pro `run_discovery` 在提供训练、验证轨迹和策略开发回调时先改进策略，并直接把选出的策略用于新在线研究。短暂平台期不会提前终止剩余预算；旧 ParallelRefine 可显式用于对照。

## 冻结对照

- 模型固定 claude-opus-5 / high；每次最多 32768 输出 token。按实际提示 UTF-8 字节数加 64 字节封装及最大输出，逐次预留费用；单次上界最多 200000 token，全实验合计预留不得超过 $975。关闭回退和重试。
- 先采集四个问题的完整树（4 分支 × 6 步），两题开发、两题验证；再冻结策略。
- 在线测试五题：外推、交互机制、动态系统预测、噪声与异常值、环境迁移。每题两次独立数据重复，新旧策略每次各 14 次尝试。均使用相同分支上下文协议，旧策略均匀分配深度。
- 模型可写完整 Python 方法或诊断实验，不设算法菜单。程序在无网络、无密钥的受限容器执行；最终标签不挂载给方法程序。
- 先固定所有最终代码，再统一评估独立留出集。按问题先求重复均值，再给五个问题等权；同时保留失败、退步、成本和耗时。
- 这是合成预测问题上的开放方法研究，不能据此宣称通用真实世界科研能力已提高。

## 时间与预算

目标至少 12 小时的实际研究，完成预先规定的对照块，不根据收益提前结束，也不空等凑时长。若预定调用或预算先耗尽，将报告实际时长。

核验旧 ML V2 任务已关闭后，释放从未发出的预留 $981.058675；其 19 次既有请求仍保留完整输入与最大输出覆盖。本轮预留 $975（接口预检与正式运行合计最多 390 次调用，同时执行总金额限制），累计保守预留 $4988.4727 / $5000。历史未知费用不被清零。真实费用另从 usage 记录汇总。

本地完整计划、输入、源码、调用日志和执行结果位于工作目录中的 `dream-rsi-long-20260918-v2/`。结束后补充可复核汇总和最终判断。

## 预检修正

接口预检的 3 次模型调用全部返回并保留费用记录。其不完整世界不计入正式均值：首个程序仅因缺少标题被拒绝，暴露了与科研执行无关的格式门槛。正式接口只要求 `kind` 和 `source`；标题、假设和解释为可选说明。42 次已验证基线执行及相同数据按字节核验后复用，没有重新付费重放预检调用。

保留完整分支历史会使深层提示增长，因此正式运行使用总金额预留限制，替换单次固定 100000 token 限制；没有增加总费用授权。原预检目录和冻结协议完整保留。新旧在线对照使用同一修正接口。

在线每个世界的 14 次尝试相同；新策略额外使用 96 次树采样和 4 次策略开发。报告会单列这部分开发开销，不把整个学习流程表述成总调用数相同的对照。
