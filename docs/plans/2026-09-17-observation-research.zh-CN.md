# 研究能力：利用已有实验结果改进下一步

## 本轮问题与修改

此前真实响应把包含可选作业的 13 分种子称为“只有必做作业”，并继续提出容量冲突方案。
实际提示中的候选定义、评价与来源引用分散，重复路径、哈希和运行记录占用了大量内容。
本轮检验：在同模型和同预算下，压缩已核验观察的呈现，并明确评价对应的候选 ID，
能否让后续研究得到更好的有效候选、减少重复失败。

改动仅涉及模型所见的原生 CPU 观察。完整 values、validation、协议和比较范围保留；
每条观察直接列出 evaluated_inputs 的来源候选 ID。后台继续保存并重新核验原始记录，
持久记忆的来源绑定仍使用完整记录；来源有效不等于科学结果有效。
只展示当页和显式保留的记录，遗漏、未知和无效均不能被当成成功。

## 调用模型前固定的对照

- 两道新的 13 作业任务，容量为 2／3；每题两次配对，共 8 个项目。
- 每项目实际执行 12 条历史候选和评价，含 6 条合法方案及 6 条约束反例。起始最佳分数为 23／28。
- 三轮研究，每轮请求两个候选，最多三个模型请求和一次可选记忆写入。没有强制写笔记。
- A 使用完整原生审计记录；B 使用简明观察视图。双方使用相同当前代码、任务指令、初始候选及检索接口；A 仅将呈现函数替换为原样复制。
- 同一 Haiku 4.5、8192 输出额度、每进程共用 300000 token、120 秒 HTTP 超时、120 秒累计原生 CPU 预留。
- 两组联合快照均为 32768 字节；压缩可能改善可见覆盖，这正是待测机制。A 仍可使用同样的分页／定向读取接口。
- 固定质量目标分别为至少 45／55 分，不宣称是数学最优。顺序为 0A、0B、1B、1A、2B、2A、3A、3B；编号 0／2 为第一题，1／3 为第二题。
- 不按结果增加项目或替换失败；所有协议错误、超时、无效候选和重复提案保留。

主指标是独立复算后的达标项目数、最佳合法新分数、有效／无效候选、重复失败，
同时报告首次改进／达标时间、整个项目时间、请求和返回用量。只减少提示字节或增加可见记录，
不能直接算成研究能力提升。四对探索不足以证明通用收益；这仍是单领域，跨领域验收保持未完成。

[执行前计划](../evidence/runs/2026-09-17-observation-research/plan.json)固定任务、顺序、阈值和源码哈希。
每个项目使用正常 Pro 许可、真实远端模型和原生 CPU 工作进程，直接调用实际 research consumer；
本轮不是生产上线或完整 CLI 验收。

本轮保守预留 24 美元，累计预留从 347 增至 **371 美元**，用户授权上限仍为 **5000 美元**。
8 × 300000 token × 5 美元／百万 token 的上界为 12 美元，额外保留同额余量。
预留不代表消费。开始前累计已知返回用量估算为 3.909833 美元，另有 14 次费用未知请求，均不重置。

## 结果

首批在原生队列决策处中断：已启动 3 个项目，各完成一轮模型调用及第一个新候选评价；另 5 个没有启动。
只读复现确认 `attempt_json_complexity_limit` 来自默认队列复制其不使用的全局历史，并非模型协议错误。
三个已评价新候选分别为 56、35、22 分；其余未完成评价与轮次保持未知，不能把这部分作完整 A/B 结论。

最小修复让内置队列只使用任务队列与预算，不构建无用的历史视图；自定义决策仍获得原有证据。
选中输入的真实内容、来源和执行准入仍检查。69 项相关检查通过，包括真实有效输入执行及损坏输入拒绝。

[修复后固定计划](../evidence/runs/2026-09-17-observation-research/plan-v2.json)保留同样任务、历史、阈值、模型与顺序，
双方共同使用队列修复，完整重做 8 个项目；遇到新的执行器失败即停止后续启动并保留未运行项。
这属于同任务后续探索，不能当作新的独立盲测。首批全部原件与费用保留，不用复跑替换失败。
新增保守预留 24 美元，累计预留 **395 美元**；首批未使用的预留不抵扣。

### 完整后续对照：默认简化未通过研究质量检验

8 个项目全部完成，24 个真实模型请求均返回。所有已执行候选均从原始排程字节独立复算。
源码固定为 Core `d40eda9`、Pro `90da3dc`，完整计划、响应、执行失败、来源与用量见
[结构化汇总](../evidence/runs/2026-09-17-observation-research/observation-research-20260917-v2-summary.json)。

| 指标 | A 完整视图 | B 简明视图 |
| --- | ---: | ---: |
| 达到执行前固定目标 | 3/4 | 1/4 |
| 四组最佳新分数（对应目标 45、55、45、55） | 43、59、58、55 | 29、50、42、55 |
| 新候选合法／无效 | 14／8 | 18／4 |
| 严格优于种子的不同合法排程 | 14 | 14 |
| 重复已评价排程／保留旧提案 | 0／0 | 1／2 |
| 完整进程时间合计，含历史建立 | 616.268 秒 | 606.714 秒 |
| 首次达标时间，未达标记未知 | —、100.428、74.967、75.786 秒 | —、—、—、151.530 秒 |
| 返回用量估算费用 | $0.265586 | $0.247752 |
| 实际持久记忆写入 | 0 | 0 |

A 有一次研究响应协议失败；B 没有。这些失败及无效候选均纳入表中。
简明视图的候选合法率更高，但三组最佳质量更低、一组持平，达标数下降。
完整时间和费用的小幅减少不能当成等质量提效；唯一双方达标的配对中，B 首次达标约晚一倍。
这些是四对单领域探索的观察，不作总体显著性或“压缩必然有害”的结论。

实际首轮完整提示约 49.58 KB，两组几乎一样；联合快照上限仍是 32 KiB。
A 首轮有 6 条合格报告，全部为有效结果；B 有 12 条，包含 6 条有效和 6 条无效结果。
因此实际干预同时改变呈现和可见覆盖，不能把差异只归因于可读性，
更不能从单条记录缩短推导整条提示节省一半。双方仍可分页取回其他证据。

**处理决定：撤回 Pro 的默认简明视图，保留完整视图。** `90da3dc` 保存实验实现，
`6f47b74` 完整撤回这次呈现修改；没有留一个未经证明的配置分支。
Core 内置队列不再复制无用全局历史的最小修复保留：它解决已复现的执行阻断，
并不据此声称提高了解题质量。恢复默认后的 6 项实际原生反馈兼容检查通过，另存于证据目录。

### 下一项能力检验：实际研究中的可比测量与确认实验

只读审计现有 ASR 项目的配置和指标后发现：probe 与全量汇总排序相反，
部分 probe 样本数不同，原始输出与后处理评分也不应混用。
接着在执行前固定仅检查唯一数值训练变化所对应的数据集，对两份候选的全部八个既有分片复算。
参考文本按分片位置一致，每条词编辑距离均由独立动态规划核对；原始 16 份文件哈希前后相同。
这不是重新识别音频，也没有确认音频 ID、模型权重和完整归一化来源，不能据此作因果或显著性结论。
项目内容、具体分数和复算证据保存在私有 Pro 仓库；研究项目自身未修改。

下一轮据此冻结真实决策案例：模型能否识别可比结果、明确尚缺证据，并选择最小确认实验。
不再继续为排程题微调提示。原定跨领域、长程和等质量收益验收仍保持未完成。

## 核验与账目

- Pro 候选相关检查最终 111 项通过；最后一处说明文字另做 37 项联合快照检查。此前两次测试夹具失败日志完整保留，不相加重复测试数。
- Core 队列修复 69 项通过，覆盖自定义策略证据与合法／损坏原生输入；未提高 JSON 限制或放松来源准入。
- [首批中断汇总](../evidence/runs/2026-09-17-observation-research/observation-research-20260917-summary.json)保留 3 个已启动及 5 个未启动项目；第三个项目的外层进程时间未知，不补造。
- 本轮首批与后续合计 27 个返回请求，已知增量 $0.576976；累计 **248 个准备请求、234 个返回、14 个费用未知**，已知返回用量估算 **$4.486809**。这是价格表估算，不是账单，基础设施成本未计入。
- 累计保守预留 **$395 / $5000**；预留不是消费，未启动项目的预留没有回收。
- [归档清单](../evidence/runs/2026-09-17-observation-research/archives.json)和逐文件哈希包含两批完整原件；复现脚本依赖当时固定的 Pro 候选提交，不能直接套用已撤回后的主线。
